2006-10-13  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/site/common.in: Changed location of forums from "forum" to
	"Forum", because of increasing user registration spam.

2006-09-14  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.26.

	* doc/cart-mbipg-EO.png: Regenerated.
	* doc/cart-mbipg-EO.pdf: Regenerated.

2006-09-14  Darwin Slattery <darwin.slattery@ul.ie>

	* doc/cart-mbipg-EO.eps: replaced plot with corrected version.

2006-09-14  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/site/Makefile: Changed location of site to match pyrrha
	location.
	* doc/site/news.in: Updated.

	* doc/cart-mbipg-GALIB.txt: New version.
	* doc/cart-mbipg-GALIB.pdf: New version.
	* doc/cart-mbipg-GALIB.png: New version.
	* doc/cart-mbipg-EO.txt: New version.
	* doc/cart-mbipg-EO.pdf: New version.
	* doc/cart-mbipg-EO.png: New version.
	* doc/spiral-mbipg-GALIB.txt: New version.
	* doc/spiral-mbipg-GALIB.pdf: New version.
	* doc/spiral-mbipg-GALIB.png: New version.
	* doc/spiral-mbipg-EO.txt: New version.
	* doc/spiral-mbipg-EO.pdf: New version.
	* doc/spiral-mbipg-EO.png: New version.

	* doc/cartcentering.texi: Added @page at end of file.

	* doc/cartcentering.texi: Corrected node structure.
	* doc/intertwinedspirals.texi: Corrected node structure.

	* .cvsignore: Added libGE.log .

2006-09-13  Darwin Slattery <darwin.slattery@ul.ie>

	* doc/cartcentering.texi, doc/intertwinedspirals.texi: added
	performance section, improved formatting and re-phrased a number of
	sections.
	* EXAMPLES/CartCentering: general changes to improve conformance with
	other examples.
	* EXAMPLES/IntertwinedSpirals: general changes to improve conformance
	with other examples.
	
2006-09-12  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/allclasses.*: Updated with latest changes.

2006-08-25  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/site/common.in: Replaced all hard links to waldo by local links.
	Updated BDS website link.
	* doc/site/documentation.in: Replaced all hard links to waldo by local
	links.

2006-08-23  Miguel Nicolau  <miguel.nicolau@ul.ie>
	
	* doc/install.texi: Removed "gnuzip" command example.
	* README: Regenerated from doc/README.texi (makeinfo --plaintext).

	* doc/libGE.texi: All class images are non-scaled now.
	* doc/Genotype.fig: Renamed private members to match source code.
	* doc/Genotype.eps: Corresponding changes.
	* doc/Genotype.pdf: Corresponding changes.
	* doc/Genotype.png: Corresponding changes.
	* doc/Genotype.txt: Corresponding changes.

	* doc/libGE.texi: Removed method c_str() from Phenotype class
	description.
	* src/Phenotype.h: Private attributes valid and fitness renamed to
	_libGE_Phenotype_valid and _libGE_Phenotype_fitness, respectively.
	* src/Phenotype.cpp: Corresponding changes.
	* doc/Phenotype.fig: Renamed private members to match source code;
	removed method c_str().
	* doc/Phenotype.eps: Corresponding changes.
	* doc/Phenotype.pdf: Corresponding changes.
	* doc/Phenotype.png: Corresponding changes.
	* doc/Phenotype.txt: Corresponding changes.

	* doc/Rule.*: Small formatting changes.
	* doc/Production.*: Small formatting changes.
	* doc/Symbol.*: Small formatting changes.
	* doc/Tree.*: Small formatting changes.
	* doc/CFGrammar.*: Small formatting changes.
	* doc/GEGrammar.*: Small formatting changes.
	* doc/GEGrammarSI.*: Small formatting changes.

	* doc/libGE.texi: Updated description of Symbol class to match code
	comments.

	* doc/libGE.texi: Added entries for new setStartSymbol() methods in
	description of Grammar class.
	* doc/Grammar.fig: Added startSymbol attribute and new
	setStartSymbol() methods.
	* doc/Grammar.txt: Corresponding changes.
	* doc/Grammar.eps: Corresponding changes.
	* doc/Grammar.pdf: Corresponding changes.
	* doc/Grammar.png: Corresponding changes.

	* doc/libGE.texi: In Initialiser class subsubsection: updated
	description; renamed attribute seedsPos to popIndex; removed
	attributes randomSeed and seeds; removed methods getRandomSeed(),
	setRandomSeed() and initSeeds().
	* doc/initialiser.*: Corresponding changes.

	* doc/libGE.texi: In subsection <GECodonValue>, added paragraph
	explaining use of ranges.

	* configure.ac: Changed version to 0.26.
	* doc/site/common.in: Updated for new version.
	* doc/site/news.in: Updated for new version.

	* TESTS/*: Regenerated all test files for 0.26.
	* TESTS/CFG_READING/script.*: New versions.
	* TESTS/PHENOTYPE/script.*: New versions.
	* TESTS/SYMBOL/script.*: New versions.
	* TESTS/TREE/tree.cpp: Main function now returns 0 upon success.
	* TESTS/TREE/script.*: New versions.

2006-08-21 Darwin Slattery <darwin.slattery@ul.ie>
	
	* EXAMPLES/CartCentering/GE_EO: added missing file main.cpp to CVS.

2006-08-18  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.26beta2.

	* src/CFGrammar.cpp: (pointed out by Sebastien Piccand) Include of
	<fstream>.
	* Makefile.am: (pointed out by Michael O'Neill) EXTRA_DIST now
	includes all the Makefiles for the EXAMPLES directory.
	* configure.ac: Changed version to 0.26beta2.
	* doc/site/Makefile: Before exporting the tar.gz of code to www
	directory, run configure script with GE_ILLIGALSGA and GE_MITGALIB
	disabled, so that includes are not declared inside libGEdefs.h.

	* doc/libGE.texi: Changed years of copyright to 2003-2006.

	* doc/site/common.in: Updated for new version.
	* doc/site/news.in: Updated for new version.

2006-08-17  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.26beta1.

	* TESTS/SantaFeAntTrail/GE_EO: Updated test files for 0.26beta1.

	* doc/libGE.texi: Added author line with Darwin Slattery.

	* doc/site/common.in: Updated for new version.
	* doc/site/news.in: Updated for new version.

	* doc/readme.texi: Renamed to intro.texi.
	* doc/intro.texi: New version.
	* doc/README.texi: Now includes intro.texi.
	* doc/libGE.texi: Now includes intro.texi.

	* README: Regenerated from doc/README.texi (makeinfo --plaintext).

	* configure.ac: Changed Miguel's e-mail to gmail account.
	* doc/intro.texi: Changed Miguel's e-mail to gmail account.

2006-08-16 Darwin Slattery <darwin.slattery@ul.ie>

	* EXAMPLES/*/GE_EO/Makefile: set a default EO_LIBDIR path for EO .a
	files in all examples to make sure valid paths were pased to the
	linker.
	* EXAMPLES/*/*/Makefile: removed variables for external library
	include and lib directories so user only has to edit CXXFLAGS and
	LDFLAGS now.
	* EXAMPLES/SantaFeAntTrail/GE_EO/Makefile: fixed gegcc object target
	rules.
	
2006-08-15  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/cartcentering.texi: Fixed node structure, minor reference
	changes.
	* doc/santafeanttrail.texi: Renamed some nodes.
	* doc/libGE.texi: Node headers for each of the examples is now
	declared inside this file.
	
	* Makefile.am: Inclusion of specific directories for EXAMPLES, to
	avoid inclusion of CVS directories.

	* TESTS/SantaFeAntTrail/GE_ILLIGALSGA: Removed.
	* TESTS/SantaFeAntTrail/GE_MITGALIB/script.*: Now work in similar
	fashion to GE_EO scripts.
	* TESTS/SantaFeAntTrail/GE_EO/script.create: Changed executable to
	GESLANG.

2006-08-15 Darwin Slattery <darwin.slattery@ul.ie>

	* doc/cartcentering.texi: fixed invalid math symbols in cart centering
	example documentation.
	* EXAMPLES/CartCentering/*/main.cpp: modified code to conform to new
	libGE API changes regarding random number generator etc.
	* EXAMPLES/IntertwinedSpirals/*/main.cpp: modified code to conform to
	new libGE API changes regarding random number generator etc.
	* EXAMPLES/IntertwinedSpirals/*: added gcc, tcc, libTCC, and lua
	backends to the EO and GALib versions.
	* EXAMPLES/SanteFeAnTrail/*/Makefile: modified Makefiles in EO and
	GALib versions to conform to other examples. 
	* EXAMPLES/SanteFeAnTrail/*/main.cpp: modified code to conform to new
	libGE API changes regarding random number generator etc.

2006-08-14  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* Makefile.am: EXTRA_DIST for examples now includes each directory
	specifically, to avoid inclusion of CVS directory.

	* TESTS/SI/si.cpp: Removed call to setRandomSeed().

	* src/GRGrammarSI.cpp: In method growTree(), only starting substring
	"<GECodonValue" is tested when undefined NTSymbol is found.
	* src/GRGrammar.cpp: In method genotype2PhenotypeStep(), correct
	calculation of codon value when using GECodonValue.


	* TESTS/testgrammars/GECodonValue1.bnf: Inserted spaces between
	symbols.

	* TESTS/*/Makefile: Now compile to local libGE.a instead of global.

	* src/CFGrammar.cpp: In method addBNFString(char*), returns false if
	new string could not be appended, after restoring previous state of
	mapper.

	* TESTS/SI/si.cpp: Now follows the standard approach of the other
	examples.

	* TESTS/STARTSYMBOL/startsymbol.cpp: Removed potential for core dumps.
	Slight changes.

2006-08-11  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GE_ILLIGALSGA.cpp: In function GE_ILLIGALSGA_getPhenotype(),
	inserted a test to ensure memory was properly allocated for
	c_phenotype pointer.
	* src/GE_ILLIGALSGA.cpp: Now returns size+1, to count null char.

	* src/Initialiser.h: Removed getRandomSeed(), setRandomSeed() and
	initSeeds() methods, randomSeed attribute, and renamed seedsPos to
	popIndex; constructor now only takes one argument; removed unnecessary
	includes; changed class description.
	* src/Initialiser.cpp: Removed all definitions/references to removed
	methods; removed unnecessary include.
	* src/GEGrammarSI.cp: In method init(), removed all use of state
	manipulation functions initstate() and setstate().

	* src/CFGrammar.cpp: In copy constructor, if NTSymbol is found, a new
	symbol is created and pointed at.

2006-08-10  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Phenotype.h: Removed method c_str().
	* src/Phenotype.cpp: Removed method c_str().

2006-08-09  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Phenotype.cpp: Method c_str() no longer uses strdup().

	* src/Genotype.cpp: #include<string>.

	* src/CFGrammar.cpp: Method readBNFFile(const char*) uses full C++
	file IO.

	* src/GE_ILLIGALSGA.cpp: In method GE_ILLIGALSGA_Mapper(), array
	integers is now dynamically allocated (because of non-constant size).

	* src/*.h: Directive "using namespace std" is only used after all
	includes.

2006-08-08  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Grammar.h: New private member startSymbol.
	* src/Grammar.cpp: All constructors initialise startSymbol.
	* src/Grammar.cpp: Method getStartSymbol() now uses startSymbol.
	* src/Grammar.h: 3 new methods setStartSymbol().
	* src/Grammar.cpp: Implementation of the methods above.

	* src/GEGrammar.h: Removed variable leftToRight and methods
	getLeftToRight() and setLeftToRight().
	* src/GEGrammar.cpp: Corresponding changes.

	* src/CFGrammar.cpp: In method readBNFString(const char*), currentRule
	is initialised to NULL.
	* src/CFGrammar.cpp: Removed rulePtr from method updateRuleFields().

	* src/GEGrammarSI.h: Method growTree() uses unsigned int as arg.
	* src/GEGrammarSI.cpp: Corresponding change.
	* src/GEGrammarSI.cpp: Line 312, casting to unsigned int.

	* src/CFGrammar.cpp: In method readBNFFile(const char*), strcpy()
	replaced with strncpy().
	* src/GE_ILLIGALSGA.cpp: In method GE_ILLIGALSGA_getPhenotype(char**),
	strcpy() replaced with strncpy.
	
	* src/Initialiser.cpp: In method initSeeds(), replaced random() with
	rand().
	* src/GEGrammarSI.cpp: Replaced all calls to random() with rand().

2006-06-27  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/site/common.in: Changed location of forums from "forums" to
	"forum", because of increasing user registration spam.

2006-06-23  Miguel Nicolau <miguel.nicolau@ul.ie>

	* doc/mapper.fig: Corrected diagram (had 2 genotypes).
	* doc/mapper.eps: Same as above.
	* doc/mapper.png: Same as above.
	* doc/mapper.pdf: Same as above.

2006-05-26  Darwin Slattery <darwin.slattery@ul.ie>

	* EXAMPLES/: Added IntertwinedSpirals example directory with EO and
	GALIB implementations.
	* doc/: Added intertwinedspirals.texi documentation file and updated
	libGE.texi to include the file.
	* Set paths to allow build on beowulf.

2006-05-04  Darwin Slattery <darwin.slattery@ul.ie>

	* EXAMPLES/CartCentering: fixed compilation errors for lua 5.1 but
	causes compilation problems with older lua versions. Added
	GEListGenome class to GALIB example. Fixed Makefiles to match SanteFe
	example.
	* doc/cartcentering.texi: updated documentation to match code changes.

2006-04-27  Darwin Slattery  <darwin.slattery@ul.ie>

	* doc/cartcentering.texi: Finished documentation for the cart
	centering problem description and implementation in ILLIGALSGA/
	MITGALIB, and EO.

2006-03-31  Darwin Slattery  <darwin.slattery@ul.ie>

	* doc/cartcentering.texi: Added documentation for the cart centering
	problem description and implementation in ILLIGALSGA.

2006-03-24  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/site/links.in: (pointed out by Michael O'Neill) Updated S-Lang
	link.

2006-03-23  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: Added -pg to
	CXXFLAGS.
	* EXAMPLES/SantaFeAntTrail/GE_EO/Makefile: Added -pg to CXXFLAGS.
	* EXAMPLES/CartCentering/GE_MITGALIB/Makefile: Added -pg to CXXFLAGS.

	* Makefile.am: Created AM_CPPFLAGS with flags -Wall and -pg.
	* Makefile.am: Removed AM_CFLAGS.

2006-03-15  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_EO/Makefile: Inserted space before
	backslash linking broken compilation lines (caused error on earlier
	compilers).
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: Same as above.

	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFuncTCC.h: Now calls
	tcc_add_file() twice, for "GEant.o" and "GEtrail.o".
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-tcc.cpp: Same as above.
	* EXAMPLES/SantaFeAntTrail/GE_EO/Makefile: No longer creates file
	"GEantfuncs.o" for GETCC, instead creates separate "GEant.o" and
	"GEtrail.o"; gcc-4.0 does not allow multiple compilations with a
	single object file output.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: Same as above.

2006-03-15  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Symbol.h: Quick comment fixes.
	* doc/libGE.texi: Quick fixes on FAQ.

2006-03-14  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammar.h: Private variable leftToRight and methods
	getLeftToRight() and setLeftToRight() added.
	* src/GEGrammar.cpp: Corresponding changes.
	* src/GEGrammar.cpp: All constructors call setLeftToRight(true).
	* src/GEGrammar.cpp: Copy constructor calls setLeftToRight(copy...).

2006-03-13  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.h: Removed include of <stack>.
	* src/CFGrammar.h: Added include of <string>.

	* src/CFGrammar.h: New method readBNFString(const string&).
	* src/CFGrammar.h: New method addBNFString(const string&).
	* src/CFGrammar.cpp: Implementation of readBNFString(const string&).
	* src/CFGrammar.cpp: Implementation of addBNFString(const string&).
	* doc/libGE.texi: Updated documentation with above methods and also
	addBNFString(const char*).
	* doc/cfgrammar.txt: Updated with above methods and also
	addBNFString(const char*).
	* doc/cfgrammar.fig: Updated with above methods and also
	addBNFString(const char*).
	* doc/cfgrammar.eps: Updated with above methods and also
	addBNFString(const char*).
	* doc/cfgrammar.png: Updated with above methods and also
	addBNFString(const char*).
	* doc/cfgrammar.pdf: Updated with above methods and also
	addBNFString(const char*).
	* doc/cfgrammar.txt: Updated with addBNFString() on CFGrammar; also
	improved graphics.
	* doc/cfgrammar.fig: Updated with addBNFString() on CFGrammar; also
	improved graphics.
	* doc/cfgrammar.eps: Updated with addBNFString() on CFGrammar; also
	improved graphics.
	* doc/cfgrammar.png: Updated with addBNFString() on CFGrammar; also
	improved graphics.
	* doc/cfgrammar.pdf: Updated with addBNFString() on CFGrammar; also
	improved graphics.

2006-03-07  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/site/common.in: Changed location of forums from "phpbb2" to
	"forums", because of increasing user registration spam.

2006-03-06  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.h: New method addBNFString(char*).
	* src/CFGrammar.cpp: Implementation of addBNFString(char*).
	* TESTS/CFG_READING/cfg_reading.cpp: Included test of new method.

2006-03-03  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: In method readBNFString(const char*), new flag
	"insertRule" indicates if rule definition is an addition to existing
	rule, as in "<A>::=a\n<A>::=b".

	* doc/santafeanttrail.texi: New version.
	* doc/cartcentering.texi: New version.
	* doc/libGE.texi: Santa Fe example moved to santafeanttrail.texi, and
	include of santafeanttrail.texi and cartcentering.texi.

2006-03-02  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* BUGS: Cleared (old) contents.
	* NEWS: Updated message.
	* AUTHORS: Updated message.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: Rule "clean" also
	removes "gmon.out" file.
	* EXAMPLES/SantaFeAntTrail/GE_EO/Makefile: Rule "clean" also removes
	"gmon.out" file.
	* EXAMPLES/CartCentering/GE_MITGALIB/Makefile: Rule "clean" also
	removes "gmon.out" file.
	* EXAMPLES/CartCentering/GE_EO/Makefile: Rule "clean" also removes
	"gmon.out" file.
	
	* TESTS/DERIVATIONTREE/script.create: stdout and stderr output goes to
	diff file.
	* TESTS/INDIVIDUAL/script.create: Same as above.
	* TESTS/NEWMAPPER/script.create: Same as above.
	* TESTS/NEW_AND_DELETE/script.create: Same as above.
	* TESTS/PRODUCTIONS/script.create: Same as above.

2006-03-01  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_EO/Makefile: Modified SLANGDIR to more
	common location "/usr/local/include".
	
	* EXAMPLES/CartCentering/*: New versions.

	* EXAMPLES/CartCentering/GE_EO/Makefile: Modified all "/opt"
	directories to more common "/usr/local".
	* EXAMPLES/CartCentering/GE_EO/Makefile: Added file "result" to rule
	"clean".

2006-02-21  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Added FAQ entry concerning error when using libGE
	headers after GALib has been uninstalled or moved.
	
2006-02-20  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Mapper.h: Detailed explanation is given with #error directives
	when GALib includes are not found. Tests for compilation are now done
	on _ga_ga_h.

	* Makefile.am: Conditional include of src/GE_MITGALIB.* removed;
	deprecated functions defined in those files are no longer supported.
	* src/ge.h: Conditional include of "GE_MITGALIB.h" removed.
	
2006-02-17  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.25.

	* src/CFGrammar.cpp: In copy constructor, when execution is aborted
	due to undefined non-terminal symbols, error message is more explicit
	and exit(1) is now used (as opposed to exit(0)).

	* doc/libGE.texi: Fixed "FIXME" internal pxref references.

	* doc/cfgrammar.txt: Corrected getDerivationTree() method.
	* doc/cfgrammar.fig: Corrected getDerivationTree() method.
	* doc/cfgrammar.eps: Corrected getDerivationTree() method.
	* doc/cfgrammar.pdf: Corrected getDerivationTree() method.
	* doc/cfgrammar.png: Corrected getDerivationTree() method.

	* doc/gegrammar.txt: Added getDerivationTree(), getProductions() and
	buildDTree() methods.
	* doc/gegrammar.fig: Added getDerivationTree(), getProductions() and
	buildDTree() methods.
	* doc/gegrammar.eps: Added getDerivationTree(), getProductions() and
	buildDTree() methods.
	* doc/gegrammar.pdf: Added getDerivationTree(), getProductions() and
	buildDTree() methods.
	* doc/gegrammar.png: Added getDerivationTree(), getProductions() and
	buildDTree() methods.
	* doc/libGE.texi: In section GEGrammar, added descriptions for
	getDerivationTree(), getProductions() and buildDTree() methods.

2006-02-15  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammar.cpp: In method genotype2PhenotypeStep(), calculation
	of GECodonValue now uses ostringstream method.
	* TESTS/testgrammars: New.
	* TESTS/*/script.*: New testing scripts.

2006-02-14  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/mbipg-GALIB.eps: New version.
	* doc/mbipg-EO.eps: New version.
	* doc/cfos-GALIB.eps: New version.
	* doc/cfos-EO.eps: New version.
	* doc/mbipg-GALIB.ps: No longer present.
	* doc/mbipg-EO.ps: No longer present.
	* doc/cfos-GALIB.ps: No longer present.
	* doc/cfos-EO.ps: No longer present.
	* doc/libGE.texi: In Santa Fe Ant Trail Results section, reduced size
	of figures so that `make dvi` works.

2006-02-09  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/mbipg-GALIB.ps: New version.
	* doc/mbipg-GALIB.pdf: New version.
	* doc/mbipg-GALIB.txt: New version.
	* doc/mbipg-GALIB.png: New version.
	* doc/mbipg-EO.ps: New version.
	* doc/mbipg-EO.pdf: New version.
	* doc/mbipg-EO.txt: New version.
	* doc/mbipg-EO.png: New version.
	* doc/cfos-GALIB.ps: New version.
	* doc/cfos-GALIB.pdf: New version.
	* doc/cfos-GALIB.txt: New version.
	* doc/cfos-GALIB.png: New version.
	* doc/cfos-EO.ps: New version.
	* doc/cfos-EO.pdf: New version.
	* doc/cfos-EO.txt: New version.
	* doc/cfos-EO.png: New version.
	* doc/libGE.texi: New section for Santa Fe Ant Trail Results.

2006-02-03  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_EO/GEListGenome.cpp: New file; functions
	from operators.cpp replaced by methods inside GEListGenome.
	* EXAMPLES/SantaFeAntTrail/GE_EO/operators.cpp: No longer exists.
	* EXAMPLES/SantaFeAntTrail/GE_EO/Makefile: GEListGenome.cpp replaces
	operators.cpp.
	* EXAMPLES/SantaFeAntTrail/GE_EO/main.cpp: Removed signature of
	functions from operators.cpp.
	* EXAMPLES/SantaFeAntTrail/GE_EO/main.cpp: Now makes references to
	operators inside GEListGenome.

2006-02-02  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_EO/GEListGenome.h: New file.
	* EXAMPLES/SantaFeAntTrail/GE_EO/main.cpp: "genome" is now of type
	GEListGenome.

	* EXAMPLES/SantaFeAntTrail/GE_EO/main.cpp: New variable effectiveXO,
	and parser now accepts option "effective", to flag use of effective
	crossover; choice of crossover operator is now regulated by this.
	* EXAMPLES/SantaFeAntTrail/GE_EO/operators.cpp: New function
	effCrossover().

2006-01-11  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.25alpha1.

	* src/GEGrammar.cpp: Special non-terminal <GECodonValue> now accepts
	the specification of ranges in the form "-x" for the lower range and
	"+x" for the upper range of the codon value.

2006-01-10  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Wrote subsection "EO Example User Guide".

2006-01-09  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Wrote subsection "GALib Example User Guide".

2005-12-16  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Description of files main.cpp, initfunc.cpp,
	santafe-gcc.cpp, santafe-slang.cpp, santafe-tcc.cpp and
	santafe-lua.cpp (relating to GALib example) updated.
	* doc/libGE.texi: New subsubsection GALib Example User Guide.

2005-12-15  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: Major restructuring.
	Use of GAParameterList for GALib parameters. Simple command line
	parser for libGE parameters.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: Function
	app_init() now receives wrappingEvents as argument.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: Function
	app_init() now receives wrappingEvents as argument.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-tcc.cpp: Function
	app_init() now receives wrappingEvents as argument.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-lua.cpp: Function
	app_init() now receives wrappingEvents as argument.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/initfunc.cpp: External
	declaration of minSize and maxSize, refering to new variables from
	main.cpp.

2005-12-05  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_EO/main.cpp: In functin main_function(),
	inserted call to setGenotypeMaxCodonValue(), so that same range is
	used in mapper and in eoGE.

	* src/GEGrammarSI.cpp: In method growTree(), if symbol <GECodonValue>
	is found, insert random element into Genotype, and continue building
	the tree.

2005-12-02  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammar.h: Vector productions is now protected, for access
	from derived classes.
	* src/GEGrammar.h: Method buildDTree() is now protected, for access
	from derived classes.

2005-11-30  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Subsubsection eoGEEvalFunc.h replaced with
	eoGEEvalFuncSlang.h.
	* doc/libGE.texi: New subsubsection eoGEEvalFuncGCC.h.
	* doc/libGE.texi: New subsubsection eoGEEvalFuncTCC.h.
	* doc/libGE.texi: New subsubsection eoGEEvalFuncLua.h.
	* doc/libGE.texi: Changes to subsubsection Makefile, in subsection
	"Implementation with GE_EO".

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: All S-Lang
	errors bail out execution.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFuncSlang.h: All S-Lang
	errors bail out execution.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: `make clean` removes
	files "result" and "individual.c".
	* EXAMPLES/SantaFeAntTrail/GE_EO/Makefile: `make clean` removes files
	"a.out", "result" and "individual.c".

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-lua.cpp: Emacs flag
	corrected.

	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEMutation.h: Major reformatting.

2005-11-29  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFuncSlang.h: Variable "fit"
	is always reset to 0 in operator() at start; minor corrections.
	* EXAMPLES/SantaFeAntTrail/GE_EO/GEant.c: Same version as in
	GE_MITGALIB.
	* EXAMPLES/SantaFeAntTrail/GE_EO/GEtrail.c: Same version as in
	GE_MITGALIB.
	* EXAMPLES/SantaFeAntTrail/GE_EO/santafestart.c: Same version as in
	GE_MITGALIB.
	* EXAMPLES/SantaFeAntTrail/GE_EO/santafeend.c: Same version as in
	GE_MITGALIB.
	* EXAMPLES/SantaFeAntTrail/GE_EO/santafestart.tcc: Same version as in
	GE_MITGALIB.
	* EXAMPLES/SantaFeAntTrail/GE_EO/santafeend.tcc: Same version as in
	GE_MITGALIB.
	* EXAMPLES/SantaFeAntTrail/GE_EO/grammar-lua.bnf: Same version as in
	GE_MITGALIB.
	* EXAMPLES/SantaFeAntTrail/GE_EO/Makefile: New targets GEGCC, GETCC
	and GELUA.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFuncGCC.h: New version.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFuncTCC.h: New version.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFuncLua.h: New version.

2005-11-28  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: New subsubsection santafe-lua.cpp.
	* doc/libGE.texi: Minor corrections to subsections santafe-slang.cpp
	and santafe-tcc.cpp.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: Now uses $(CC) and
	$(CXX) as the C and C++ compiler references; also minor corrections.

	* EXAMPLES/SantaFeAntTrail/GE_EO/GEtrail.sl: Same version as in
	GE_MITGALIB.
	* EXAMPLES/SantaFeAntTrail/GE_EO/main.cpp: Replaces GEEA.cpp.
	* EXAMPLES/SantaFeAntTrail/GE_EO/GEEA.cpp: Replaced by main.cpp.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFuncSlang.h: Replaces
	eoGEEvalFunc.h.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFunc.h: Replaced by
	eoGEEvalFuncSlang.h.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEMutation.h: Minor corrections.
	* EXAMPLES/SantaFeAntTrail/GE_EO/Makefile: New target GESLANG; changes
	to reflect file naming (from above); minor corrections.
	* EXAMPLES/SantaFeAntTrail/GE_EO/Makefile: Uses -include flag to
	include the "eoGEEvalFuncSlang.h" at compilation time, for target
	GESLANG.
	* EXAMPLES/SantaFeAntTrail/GE_EO/main.cpp: No longer includes
	"eoGEEvalFunc.h".

2005-11-18  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/GEtrail.lua: New version.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/GEtrail.c: Completely
	rewritten.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/GEtrail.sl: Completely
	rewritten.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/grammar-lua.bnf: New version.

2005-11-17  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-tcc.cpp: Now uses
	tcc_set_output_type() before any compilation inside the TCC state.

	* doc/libGE.texi: Minor corrections to TinyCC subsection.
	* doc/libGE.texi: Minor corrections to Lua subsection.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: New compilation
	target GELUA, using new defined variables LUADIR And LUALIBS.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-lua.cpp: New version.

2005-11-16  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafestart.c: Minor
	corrections.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafestart.tcc: New version.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafeend.tcc: New version.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: Strings are
	used for SFstart and SFend, as opposed to char*.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: In function
	objfunc(), if running of buffer in S-Lang is unsuccessful, fitness is
	assigned value 0.

	* doc/libGE.texi: New subsubsection santafe-tcc.cpp.

2005-11-15  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: Changed GETCC target
	to compile GEant and GEtrail onto a single file.

2005-11-14  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Finished Lua subsection.
	* doc/libGE.texi: Minor corrections to S-Lang subsection.
	* doc/libGE.texi: Wrote "Using your Own Search Engine" subsection.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-tcc.cpp: New version.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: New compilation
	target GETCC, using new defined variables TCCDIR And TCCLIBS.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: SLDIR and SLLIBS are
	now SLANGDIR and SLANGLIBS.

2005-11-11  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Started Lua subsection.
	* doc/libGE.texi: Minor corrections to tinyCC subsection.

2005-11-09  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Finished tinyCC subsection.
	* doc/libGE.texi: (From bug pointed out by Michael O'Neill)
	Minor corrections to Grammars section.

2005-11-08  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Started tinyCC subsection.
	* doc/libGE.texi: Minor corrections to S-Lang subsection.

2005-11-07  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Finished S-Lang subsection.
	* doc/libGE.texi: Finished Using your own Evaluator subsection.

2005-10-20  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Finished GCC subsection; minor grammatical
	corrections.

2005-10-19  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Started writing GCC subsection; updated S-Lang
	section.

2005-10-11  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.24.

	* doc/libGE.texi: Updated FAQ entry about GALib problem when using
	different compiler versions.

2005-10-06  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: Call to
	SLadd_intrinsic_variable is now in function app_init() instead of
	objfunc().
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: Variable
	"fitness" is now global.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFunc.h: Call to
	SLadd_intrinsic_variable is now inside the constructor instead of
	operator().
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFunc.h: New private variable
	"fit".

	* EXAMPLES/SantaFeAntTrail/GE_EO/GEEA.cpp: Only print message about
	best individual once.

	* doc/libGE.texi: Updated starting paragraph of chapter "Examples".
	* doc/libGE.texi: Updated sub-section "santafe-slang.cpp" in chapter
	"Examples" to changes mentioned above.
	* doc/libGE.texi: Updated sub-section "eoGEEvalFunc.h" in chapter
	"Examples" to changes mentioned above.

2005-09-30  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFunc.h: All
	instructions "SLang_Error=0" have been replaced with
	SLang_set_error(0), to comply with slang-2.0.4.

	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFunc.h: Class now saves a
	local pointer to mapper, rather than using global variable.

	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGE.h: In method readFrom(),
	dataRead is no longer cast to "int" while reading from stream.

	* src/GEGrammar.cpp: In method genotype2PhenotypeStep(), if grammar is
	stuck in recursive rule *but a codon is consumed anyway*, mapping is
	allowed to continue (as it will be stopped later on through reaching
	the end of the individual or by reaching maximum wrapping events).

2005-09-28  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* NEWS: New version.

	* configure.ac: Replaced all GAlib with GALib.

	* configure.ac: Replaced EO support message with something more
	meaningful.

	* configure.ac: Minor corrections.

	* src/Tree.h: Minor comment corrections.
	* src/Initialiser.h: Minor comment corrections.
	* src/GEGrammarSI.cpp: Minor comment corrections.

2005-09-27  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: New subsection "GALib" in chapter "Search Engines".

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: External
	declaration of variable obj_calls.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: In function
	objfunc(), variable obj_calls is incremented.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: External
	declaration of variable obj_calls.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: In function
	objfunc(), variable obj_calls is incremented.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: Declaration of
	variable obj_calls.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: Random seed is kept
	in new variable "seed", and is used by both the mapper (as before) and
	the ga (through a call to "initialize").
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: Evolutionary cycle is
	no longer a simple call to "ga.evolve()", but explicit, with
	"ga.step()"; statistics are kept per generation, and a dot per
	generation is displayed on screen.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: All
	instructions "SLang_Error=0" have been replaced with
	SLang_set_error(0), to comply with slang-2.0.4.

	* doc/libGE.texi: Examples chapter updated with changes to Santa Fe
	example using MITGALIB.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: Target "clean" now
	removes files "sf-output.dat" and "stats".

	* src/GEGrammarSI.cpp: In method init(), error message about low
	maxDepth now outputs current maxDepth.

	* doc/libGE.texi: New subsection "S-Lang/Installation" in chapter
	"Evaluators".

2005-08-16  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: New chapters "Search Engines" and "Evaluators".
	* doc/libGE.texi: New subsection "IlliGAL sga-c" in chapter
	"Search Engines".

	* doc/readme.texi: Changed MANDRAKEVERSION to 10.2 and GCCVERSION
	to 3.4.3.

	* doc/install.texi: Corrected installation example.
	* doc/install.texi: Changed installation example to use latest version
	number of GALib.

	* src/GE_ILLIGALSGA.h: Updated all function descriptions.
	* src/GE_ILLIGALSGA.cpp: Updated all function descriptions.

2005-08-15  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammar.cpp: In method genotype2PhenotypeStep(), corrected
	error where nonterminals stack was popped twice if genotype string is
	empty but symbol requires choice.

	* src/CFGrammar.cpp: In method readBNFString(const char *), a newline
	character is always simulated at end of grammar, to allow for correct
	addition of last symbol of grammar.
	* src/CFGrammar.cpp: In method readBNFString(const char *), if last
	symbol of grammar is '\', grammar is invalid.
	* src/CFGrammar.cpp: In method readBNFString(const char *), if state
	of parser after a pass is anything other than START_OF_LINE, parsing
	has failed.
	* src/CFGrammar.cpp: In method readBNFString(const char *), if line
	defining a nonterminal symbol is empty, a symbol "" is added as the
	only production associated with that rule; this symbol is now properly
	labelled as TSymbol.

2005-08-12  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammar.cpp: In method genotype2Phenotype(const bool), if a
	genotype is empty or invalid, the mapping is still allowed to
	continue.
	* src/GEGrammar.cpp: In method genotype2Phenotype(const bool), if the
	grammar is invalid, the phenotype is cleared.
	* src/GEGrammar.cpp: In method genotype2PhenotypeStep(), if the
	genotype is empty and grammar chooses <GECodonValue>, <GECodonValue>
	is inserted as string and mapping is invalid.
	* src/GEGrammar.cpp: In method genotype2PhenotypeStep(), if the
	genotype is empty but symbol requires a choice, then symbol is inserted
	as string and mapping is invalid.
	* src/GEGrammar.cpp: In method genotype2PhenotypeStep(), if the
	genotype is empty and symbol requires no choice, then begin() is used to
	pick rule for prodIt.

	* src/CFGrammar.cpp: In method readBNFString(const char *), if a valid
	grammar is loaded, then genotype2Phenotype is called to synchronise
	the phenotype object.
	* src/CFGrammar.cpp: In method readBNFString(const char *), updated
	macro ERROR_IN_GRAMMAR, so that if an invalid grammar is loaded, then
	the phenotype is cleared and set to invalid.

2005-08-11  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Corrected section 3.4, "Designing your Own
	Mappers".

2005-07-27  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* Makefile.am: libGE_TEXINFOS used for texinfo files included by
	libGE.texi.

2005-07-26  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: In method readBNFString(const char*), DOS-style
	newline characters are now dealt with properly.

2005-07-25  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/packages.fig: "Problem Specification" is now "Evaluator".
	* doc/packages.eps: "Problem Specification" is now "Evaluator".
	* doc/packages.png: "Problem Specification" is now "Evaluator".
	* doc/packages.pdf: "Problem Specification" is now "Evaluator".
	* doc/packages.txt: "Problem Specification" is now "Evaluator".

	* doc/exgrammar.fig: Newly introduced.
	* doc/exgrammar.eps: Newly introduced.
	* doc/exgrammar.png: Newly introduced.
	* doc/exgrammar.pdf: Newly introduced.
	* doc/exgrammar.txt: Newly introduced.

	* doc/libGE.texi: Section 2.2, "Using libGE", completely rewritten.
	* doc/libGE.texi: New chapter "Grammars".

2005-07-22  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Symbol.h: Updated class description.

	* Makefile.am: EXTRA_DIST variable now holds EXAMPLES, rather
	than EXAMPLES/SantaFeAntTrail.

	* doc/libGE.texi: Small changes to examples documentation.
	* doc/libGE.texi: S-Lang URL changed to http://www.jedsoft.org
	throughout.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: Variable SLDIR
	defined with the directory containing the slang.h file.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: Compilation under
	the GESLANG target now uses "-I $(SLDIR)".
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: Slang
	library is now included locally (i.e. using "" instead of <>).
	* doc/libGE.texi: Documentation updated accordingly.

	* doc/readme.texi: Changed MANDRAKEVERSION to 10.0.
	* doc/readme.texi: Changed GCCVERSION to 3.3.2.
	* doc/readme.texi: Removed reference to "wrappers", and rephrased
	corresponding paragraph.
	* doc/readme.texi: Removed table of compatibility.
	* doc/readme.texi: Removed reference to pdf form begin pre-built.

	* doc/install.texi: Minor corrections.
	* doc/install.texi: Removed reference to "WRAPPERS" directory and
	wrappers code. Rephrased corresponding paragraph.

	* doc/libGE.texi: First chapter is now "Overview", and second chapter
	is "Introduction to libGE".

2005-07-21  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: In method
	print_individual(), removed "Best individual" message.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: In method
	print_individual(), removed "Best individual" message.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: In method main(),
	added "Best individual" message before printing best individual.

	* src/Symbol.h: Removed method operator=(const string).
	* src/Symbol.cpp: Removed implementation of method
	operator=(const string).
	* src/Symbol.h: Added templated method operator=(const T).
	* src/Symbol.h: Added implementation of method operator=(const T).

	* src/GEGrammar.cpp: In method genotype2PhenotypeStep(), corrected
	calculation of charlength of codon value for <GECodonValue> insertion.

2005-07-12  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.24beta2.

	* src/GEGrammar.cpp: In method genotype2Phenotype(const bool), all
	code refering to SSymbol insertions has been removed.
	* src/GEGrammar.cpp: In method genotype2PhenotypeStep(), if symbol
	is undefined, check if it is an extension symbol, if so include it
	on phenotype string. Symbol "<GECodonValue>" currently supported.
	* src/Symbol.h: SymbolType SSymbol removed.
	* src/Symbol.h: Class description reverted to 0.23 version.
	* src/CFGrammar.cpp: Warning about undefined non-terminal symbols
	removed.

	* src/GEGrammar.cpp: In method genotype2PhenotypeStep(), variable
	returnValue introduced. In all cases, top of "nonterminals" must be
	purged of terminal symbols, prior to returning returnValue.

2005-07-11  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Rule.cpp: Corrected debugging information.

	* src/GEGrammar.cpp: In method genotype2Phenotype(const bool), SSymbol
	<GECodonValue> is now used instead of '\c' to insert a codon value
	onto the phenotype string.

2005-07-09  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: In method readBNFString(), the size of
	currentBuffer is initialised to stream_size, improving performance
	drastically.

2005-07-07  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: In method readBNFString(), escape sequences
	are dealt with at start of loop.
	* src/CFGrammar.cpp: In method readBNFString(), escaped characters
	are NO LONGER accepted inside non-terminal symbol names.
	* src/CFGrammar.cpp: In method readBNFString(), character stream
	"buffer" replaced by String "currentBuffer" (much slower, but
	easier to maintain and overflow-safe.

2005-06-15  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: In method readBNFString(), all escaped characters
	are now accepted inside non-terminal symbol names.
	* src/CFGrammar.cpp: In method readBNFString(), removed all SSymbol
	routines.

2005-05-31  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammar.cpp: Loop emptying nonterminals stack from all
	symbols other than NTSymbol type has been moved from
	genotype2PhenotypeStep() to genotype2Phenotype(const bool).
	* src/GEGrammar.cpp: In method genotype2Phenotype(const bool), maximum
	text length required to transform codon values to chars is calculated,
	and then used to insert codon values onto phenotype (in the event of
	"\c" SSymbol objects).

	* src/CFGrammar.cpp: In method readBNFString(const char*), scanner
	has been adapted to accept escaped sequence \c, transformed into
	a SSymbol type symbol.

	* src/Symbol.h: New SymbolType SSymbol added.
	* src/Symbol.h: Changed class description accordingly.

2005-05-26  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Mapper.h: Method setGenotype(const GAListGenome<T>&) first
	removes cast attribute from genome before accessing its contents
	as GALib does not provide accessors for const GAList objects.

2005-05-06  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Tree.h: Tree is now a vector<Tree<T> >.
	* src/Tree.h: Method clear() removed.
	* src/Tree.h: In copy constructor, vector copy constructor is called;
	only private data is explicitely copied.
	* src/Tree.h: Destructor does nothing.
	* src/GEGrammar.cpp: Method buildDTree() updated to correspond to
	changes above.
	* src/GEGrammar.cpp: Method buildDTree() updated to correspond to
	changes above.
	* src/GEGrammarSI.cpp: Method growTree() updated to correspond to
	changes above.

2005-05-05  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammar.cpp: In method genotype2Phenotype(), if derivation
	tree is requested, the current one is cleared first.
	* src/GEGrammar.h: New method buildDTree().
	* src/GEGrammar.cpp: Implementation of buildDTree().

	* EXAMPLES/SantaFeAntTrail/GE_EO/GEEA.cpp: Printing of best individual
	at end of run.

2005-04-29  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.24beta1.

2005-04-28  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammar.h: New private member productions.
	* src/GEGrammar.h: Method genotype2PhenotypeStep() accepts an extra
	boolean argument, to specify building of productions vector and
	derivation tree.
	* src/GEGrammar.h: New method getProductions().
	* src/GEGrammar.cpp: Implementation of method getProductions().
	* src/GEGrammar.cpp: In method genotype2Phenotype(), productions
	vector is cleared before mapping process.
	* src/GEGrammar.cpp: In method genotype2PhenotypeStep(), productions
	are kept on productions vector, if required.
	* src/GEGrammar.cpp: Copy constructor regenerates derivationTree and
	productions vector.
	* src/GEGrammar.cpp: Methods getDerivationTree() and getProductions()
	return their corresponding data structures even if mapping wasn't
	successful.

2005-04-21  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* configure.ac: Message stating that support for EO is installed.
	* doc/allclasses.txt: Updated with method getDerivationTree
	in GEGrammar.
	* doc/allclasses.eps: Updated with method getDerivationTree
	in GEGrammar.
	* doc/allclasses.png: Updated with method getDerivationTree
	in GEGrammar.
	* doc/allclasses.pdf: Updated with method getDerivationTree
	in GEGrammar.
	* doc/gegrammar.txt: Updated with method getDerivationTree.
	* doc/gegrammar.eps: Updated with method getDerivationTree.
	* doc/gegrammar.png: Updated with method getDerivationTree.
	* doc/gegrammar.pdf: Updated with method getDerivationTree.

2005-04-09  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.23.

2005-04-04  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Updated documentation with all changes since
	version 0.22.
	* doc/genotype.txt: Updated with new methods introduced.
	* doc/genotype.eps: Updated with new methods introduced.
	* doc/genotype.png: Updated with new methods introduced.
	* doc/genotype.pdf: Updated with new methods introduced.
	* doc/mapper.txt: Updated with new methods introduced.
	* doc/mapper.eps: Updated with new methods introduced.
	* doc/mapper.png: Updated with new methods introduced.
	* doc/mapper.pdf: Updated with new methods introduced.
	* doc/symbol.txt: Updated with new methods introduced.
	* doc/symbol.eps: Updated with new methods introduced.
	* doc/symbol.png: Updated with new methods introduced.
	* doc/symbol.pdf: Updated with new methods introduced.
	* doc/cfgrammar.txt: Updated with new methods introduced.
	* doc/cfgrammar.eps: Updated with new methods introduced.
	* doc/cfgrammar.png: Updated with new methods introduced.
	* doc/cfgrammar.pdf: Updated with new methods introduced.
	* doc/GE_MITGALIB.texi: Warning about deprecated interfaces.

	* doc/libGE.texi: Removed subsections for files GEant.c, GEtrail.c,
	GEant.sl, GEtrail.sl, santafestart.c and santafeend.c.
	* doc/libGE.texi: Removed verbatim inclusion of Example source files.

	* src/Mapper.h: GALib arguments to setGenotype() methods are now
	classified as 'const'.
	* src/Mapper.h: Updated method descriptions.

	* src/CFGrammar.cpp: Updated method descriptions.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/Makefile: Use of "@" prefix
	to echo command, to avoid display of echo command when Makefile is
	used without arguments.

2005-03-18  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.23beta3.

	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGE.h: Include of <GE/ge.h> removed.

	* EXAMPLES/SantaFeAntTrail/GE_EO/GEEA.cpp: Default value for sensible
	initialisation is now "false".

2005-03-17  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_EO/GEEA.cpp: New command-line argument
	to speficy which grammar file to use.
	* EXAMPLES/SantaFeAntTrail/GE_EO/GEEA.cpp: New command-line argument
	to speficy number of wrapping events.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFunc.h: Constructor now
	accepts arguments to set grammarFile and wrappingEvents.

	* src/CFGrammar.h: New method readBNFFile(const string&).
	* src/CFGrammar.cpp: Implemented method above.
	* src/CFGrammar.cpp: Updated method debug messages.

2005-03-16  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammar.cpp: In method genotype2Phenotype(bool), effective
	size of genotype is set to 0 by default.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGE.h: New methods
	getEffectiveSize() and setEffectiveSize();
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGE.h: In default constructor,
	setEffectiveSize(0) is used.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFunc: In operator(),
	effective size of genome is updated from genotype's effectiveSize.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEQuadCrossover: New methods
	getUseEffectiveCrossover() and setUseEffectiveCrossover().
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEQuadCrossover: Default
	constructor takes on optional bool argument to set use of effective
	crossover.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEQuadCrossover: In operator(),
	iterators after_cut1 and after_cut2 are chosen depending on whether or
	not effective crossover is used.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEQuadCrossover: In operator(),
	implemented correct vector copying from intermediate vectors (temp1
	and temp2).

2005-03-15  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammar.h: New method genotype2Phenotype(bool).
	* src/GEGrammar.cpp: Method genotype2Phenotype() now calls
	genotype2Phenotype(bool=false).
	* src/GEGrammar.cpp: Method genotype2Phenotype(bool) implements
	mapping process, and updates derivationTree if argument is true.
	* src/CFGrammar.h: Method getDerivationTree() is now pure virtual.
	* src/CFGrammar.cpp: Removed code for getDerivationTree().
	* src/GEGrammar.h: New method getDerivationTree().
	* src/GEGrammar.cpp: Method getDerivationTree() calls method
	genotype2Phenotype(true).

2005-03-14  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.23beta2.

	* src/GE_ILLIGALSGA.cpp: In function GE_ILLIGALSGA_getPhenotype(),
	local string object is used to build phenotype (faster, less code,
	and boundary-checked).

2005-03-11  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Symbol.h: New method bool operator==(const Symbol compSymbol).
	* src/Symbol.cpp: Implementation of the above method.

	* src/GE_ILLIGALSGA.cpp: In function GE_ILLIGALSGA_getPhenotype(),
	string_size_required is initialised to 2, and string is null
	terminated before returning it.

2005-03-08  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.23beta1.

	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEEvalFunc.h: First version.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGE.h: First version.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEInit.h: First version.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEMutation.h: First version.
	* EXAMPLES/SantaFeAntTrail/GE_EO/eoGEQuadCrossover.h: First version.
	* EXAMPLES/SantaFeAntTrail/GE_EO/GEant.h: First version.
	* EXAMPLES/SantaFeAntTrail/GE_EO/GEEA.cpp: First version.
	* EXAMPLES/SantaFeAntTrail/GE_EO/GEtrail.sl: First version.
	* EXAMPLES/SantaFeAntTrail/GE_EO/grammar.bnf: First version.
	* EXAMPLES/SantaFeAntTrail/GE_EO/Makefile: First version.
	* EXAMPLES/SantaFeAntTrail/GE_EO/santafe.trl: First version.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: In function
	objfunc(), replaced "char buffer[1000000]" with "string buffer", and
	adjusted all buffer manipulations throughout function.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/operators.cpp: Type "unsigned"
	replaced with "unsigned int" throughout.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: call to the method
	ga.pReplacement(1.0), to set full population overlap in steady-state.

	* src/Tree.h: All calls to methods begin(), end() and clear()
	refering to those methods from the vector<T> class are now preceeded
	with the qualifier vector<Tree<T>*>:: , to comply with gcc 3.4.1.

2005-02-03  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Genotype.h: Default value of "valid" on all constructors is
	true.

	* src/Genotype.cpp : Implemented cleaner constructor from array of
	integers.

	* src/Genotype.h : New constructor using vector of CodonType as input.
	* src/Genotype.cpp : Implementation of the above.

2005-01-31  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Genotype.h : Replaced attribute valid with
	_libGE_Genotype_valid;
	* src/Genotype.h : Replaced attribute fitness with
	_libGE_Genotype_fitness;
	* src/Genotype.h : Replaced attribute maxCodonValue with
	_libGE_Genotype_maxCodonValue;
	* src/Genotype.h : Replaced attribute effectiveSize with
	_libGE_Genotype_effectiveSize;
	* src/Genotype.h : Replaced attribute wraps with
	_libGE_Genotype_wraps;
	* src/Genotype.cpp : Changed all accessors according to changes
	listed above.

2004-12-15  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_ILLIGALSGA/app.c: Test and
	definition of _APP_C_ and Emacs definition line.
	* EXAMPLES/SantaFeAntTrail/GE_ILLIGALSGA/GEant.c: Test and
	definition of _GEANT_C_ and Emacs definition line.
	* EXAMPLES/SantaFeAntTrail/GE_ILLIGALSGA/GEtrail.c: Test and
	definition of _GETRAIL_C_ and Emacs definition line.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/GEant.c: Test and
	definition of _GEANT_C_ and Emacs definition line.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/GEtrail.c: Test and
	definition of _GETRAIL_C_ and Emacs definition line.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/initfunc.cpp: Test and
	definition of _INITFUNC_CPP_ and Emacs definition line.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: Test and
	definition of _MAIN_CPP_ and Emacs definition line.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/operators.cpp: Test and
	definition of _OPERATORS_CPP_ and Emacs definition line.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: Test and
	definition of _SANTAFE_GCC_CPP_ and Emacs definition line.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: Test and
	definition of _SANTAFE_SLANG_CPP_ and Emacs definition line.

2004-12-14  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Mapper.h: Preprocessor test of variable GALIB_INCLUDEPATH to
	include implementation of GALib specific setGenotype() methods.

	* configure.ac: Failure to find GALib headers or libs will only cause
	warnings, not errors.
	* configure.ac: Redefinition of GE_MITGALIB to xfalse if GALib not
	found.
	* configure.ac: Moved AM_CONDITIONAL calls to end of file, to use
	the (possibly) new value of GE_MITGALIB.
	* configure.ac: Final message indicating which wrappers were
	installed, and giving instructions on how to compile libGE.

2004-12-09  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Inserted FAQ entry about error related to ARRAY_TYPE
	definition on both Slang and GALib.

2004-11-30  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Mapper.h: Preprocessor test of the variable GALIB_INCLUDEPATH
	to include GALib include files.
	* src/Mapper.h: New methods setGenotype(GA1DArrayGenome<T>&) and
	setGenotype(GAListGenome<T> &), and preprocessor test of the variable
	GALIB_INCLUDEPATH to include these on compilation.
	* src/Mapper.h: Templated implementation of the two above methods.
	* src/GE_MITGALIB.cpp: Deprecated warning message inserted on both
	GE_MITGALIB_AssignGenotype() methods.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: Now uses
	method setGenotype(GAListGenome<T> &) on both objfunc() and
	print_individual().
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: Now uses
	method setGenotype(GAListGenome<T> &) on both objfunc() and
	print_individual().

2004-11-26  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* configure.ac: Use of AH_TOP and AC_PACKAGE_VERSION to define
	symbol libGEVersion on libGEdefs.h.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: Example of how
	to check version of libGE.

2004-11-22  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.22.

	* doc/libGE.texi: Updated documentation with all changes since
	version 0.21.
	* doc/libGE.texi: Inserted '@sp 1' before each call to '@image',
	to ensure web generated pages do not insert images in the middle
	of the previous paragraph.
	* doc/allclasses.txt: Updated with new methods introduced.
	* doc/allclasses.eps: Updated with new methods introduced.
	* doc/allclasses.png: Updated with new methods introduced.
	* doc/allclasses.pdf: Updated with new methods introduced.
	* doc/cfgrammar.txt: Updated with new methods introduced.
	* doc/cfgrammar.eps: Updated with new methods introduced.
	* doc/cfgrammar.png: Updated with new methods introduced.
	* doc/cfgrammar.pdf: Updated with new methods introduced.
	* doc/gegrammarsi.txt: Updated with new methods introduced.
	* doc/gegrammarsi.eps: Updated with new methods introduced.
	* doc/gegrammarsi.png: Updated with new methods introduced.
	* doc/gegrammarsi.pdf: Updated with new methods introduced.
	* doc/genotype.txt: Updated with new methods introduced.
	* doc/genotype.eps: Updated with new methods introduced.
	* doc/genotype.png: Updated with new methods introduced.
	* doc/genotype.pdf: Updated with new methods introduced.
	* doc/initialiser.txt: Updated with new methods introduced.
	* doc/initialiser.eps: Updated with new methods introduced.
	* doc/initialiser.png: Updated with new methods introduced.
	* doc/initialiser.pdf: Updated with new methods introduced.
	* doc/tree.txt: Updated with new methods introduced.
	* doc/tree.eps: Updated with new methods introduced.
	* doc/tree.png: Updated with new methods introduced.
	* doc/tree.pdf: Updated with new methods introduced.

	* doc/allclasses.txt: Buffered top and bottom with white lines.
	* doc/cfgrammar.txt: Buffered top and bottom with white lines.
	* doc/g2p-ge.txt: Buffered top and bottom with white lines.
	* doc/ge-ex.txt: Buffered top and bottom with white lines.
	* doc/gegrammarsi.txt: Buffered top and bottom with white lines.
	* doc/gegrammar.txt: Buffered top and bottom with white lines.
	* doc/genotype.txt: Buffered top and bottom with white lines.
	* doc/ge.txt: Buffered top and bottom with white lines.
	* doc/ge.txt: Replaced '\' and '/' symbols with '|'.
	* doc/grammar.txt: Buffered top and bottom with white lines.
	* doc/initialiser.txt: Buffered top and bottom with white lines.
	* doc/maper.txt: Buffered top and bottom with white lines.
	* doc/packages.txt: Buffered top and bottom with white lines.
	* doc/packages.txt: Replaced '\' and '/' symbols with '|'.
	* doc/phenotype.txt: Buffered top and bottom with white lines.
	* doc/production.txt: Buffered top and bottom with white lines.
	* doc/rule.txt: Buffered top and bottom with white lines.
	* doc/symbol.txt: Buffered top and bottom with white lines.
	* doc/tree.txt: Buffered top and bottom with white lines.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: Function
	print_individual now outputs genotype contents, as well as its total
	and effective sizes, and the number of wrapping events.

	* src/GEGrammarSI.h: Method growTree() now uses argument of type
	DerivationTree rather than Tree<const Symbol*>.

2004-11-16  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.22beta4.

	* src/GEGrammarSI.h: New private attributes tailRatio and tailSize.
	* src/GEGrammarSI.h: New methods getTailRatio(), setTailRatio().
	getTailSize() and setTailSize().
	* src/GEGrammarSI.cpp: Implementation of getTailRatio(),
	setTailRatio(), getTailSize() and setTailSize().
	* src/GEGrammarSI.cpp: All constructors call setTailRatio(0.0) and
	setTailSize(0) for initialisation; copy constructor copies values from
	copy argument.
	* src/GEGrammarSI.cpp: In method init(), tails are appended to the
	created genotype structure according to the tailRatio or tailSize
	parameters.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: Example calls to
	mapper.setTailSize() and mapper.setTailRatio().

	* src/Rule.cpp: Copy constructor now uses methods getRecursive() and
	getMinimumDepth() rather than accessing the attributes directly.

	* src/CFGrammar.cpp: Copy constructor empties contents of phenotype
	and sets its validity to false.
	* src/GEGrammar.cpp: Copy constructor calls genotype2Phenotype() to
	regenerate correct phenotype structure.
	* src/CFGrammar.cpp: Error message stating that copying of grammars
	with non-defined non-terminal symbols is not possible.

2004-11-07  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.22beta3.

	* src/CFGrammar.cpp: In function isRecursive(), non-defined
	non-terminal symbols are considered as terminal symbols, as far as
	recursiveness and minimumDepth are concerned.

	* src/GEGrammarSI.cpp: In growTree(), if no definition for the current
	non-terminal symbol is found, it is treated as a TSymbol, but both
	genotype and phenotype are set to false.

	* src/GEGrammarSI.cpp: In init(), if the genotype2Phenotype() mapping
	is unsuccessful, a warning is produced to stderr, but mapping
	continues on.

2004-11-05  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_ILLIGALSGA/app.c: Include of <GE/ge.h>.

	* src-CFGrammar.cpp: (from bug pointed out by Dan Costelloe)
	In function isRecursive(), removed update of
	currentRule->getMinimumDepth() in the middle of the iteration through
	the symbols of each production.

	* src/CFGrammar.h: (from bug pointed out by Dan Costelloe)
	Method genotype2PhenotypeStep() now returns an integer.
	* src/CFGrammar.cpp: Changes in genotype2PhenotypeStep() to return
	number of codons consumed by mapping step, -1 if unsuccessful.
	Also, no longer updates genoIt!
	* src/CFGrammar.cpp: Changes in genotype2Phenotype() to reflect
	changes above.
	* src/CFGrammar.cpp: In genotype2Phenotype(), newEffectiveSize is
	incremented as codons from genotype are used by
	genotype2PhenotypeStep().

	* src/GEGrammarSI.cpp: In init(), a startRule->getMinimumDepth()
	bigger or equal (rather than bigger) than getMaxDepth() results in
	failure.

2004-11-02  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* configure.ac: Emacs definition for top of ge.h added through
	call to AH_TOP.

2004-10-25  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GE_ILLIGALSGA.cpp: In function GE_ILLIGALSGA_getPhenotype,
	variable string_size_required starts at 1 rather than 0.

2004-10-19  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.22beta2.

	* src/CFGrammar.h: Defined custom type DerivationTree.
	* src/CFGrammar.h: Corresponding changes.
	* src/CFGrammar.cpp: Corresponding changes.
	* src/GEGrammarSI.cpp: Corresponding changes.

	* src/GEGrammarSI.h: New constructor with Genotype structure as
	argument.
	* src/GEGrammarSI.h: New constructor with Phenotype structure as
	argument.
	* src/GEGrammarSI.cpp: Corresponding changes.

	* src/Genotype.cpp: Copy constructor duplicates all static values
	from argument structure.
	* src/Phenotype.cpp: Copy constructor duplicates all static values
	from argument structure.
	* src/Grammar.cpp: Copy constructor duplicates all static values
	from argument structure.
	* src/Grammar.cpp: In copy constructor, methods getType() and
	setType() are now used, rather than direct references to the
	attributes.
	* src/GEGrammarSI.cpp: Copy constructor duplicates all static values
	from argument structure.
	* src/Tree.h: Copy constructor duplicates all static values
	from argument structure.

	* src/Tree.h: Methods getValid() and setValid() introduced.
	* src/Tree.h: Corresponding changes across all methods.

	* src/Tree.h: Updated method descriptions.

2004-10-18  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Genotype.h: New private attribute wraps.
	* src/Genotype.h: New methods getWraps() and
	setWraps().
	* src/Genotype.cpp: Implementation of getWraps()
	and setWraps().
	* src/Genotype.cpp: Constructors all set wraps to 0.
	* src/Genotype.cpp: Copy constructor copies wraps
	from argument.
	* src/GEGrammar.cpp: In method genotype2Phenotype(), variable
	wraps is used in genotype.setWraps(wraps), after the mapping process.

	* src/Genotype.h: New private attribute effectiveSize.
	* src/Genotype.h: New methods getEffectiveSize() and
	setEffectiveSize().
	* src/Genotype.cpp: Implementation of getEffectiveSize()
	and setEffectiveSize().
	* src/Genotype.cpp: Constructors all set effectiveSize to 0.
	* src/Genotype.cpp: Copy constructor copies effectiveSize
	from argument.
	* src/GEGrammar.cpp: In method genotype2Phenotype(), variable
	effectiveSize is updated during mapping process, and its value is
	assigned to genotype upon completion of mapping.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: (from bug pointed
	out by Darwin Slattery)
	mapper.setRandomSeed() is now called with the seed passed as argument
	to the program, if it exists.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: Function
	print_individual now outputs genotype contents, as well as its total
	and effective sizes, and the number of wrapping events.

2004-10-14  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.22beta1.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/initfunc.cpp: (from bug pointed
	out by Darwin Slattery)
	Two copies of initial codon were being inserted at start of genome;
	Genotype iterator is now incremented after the first insert.

2004-10-13  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* configure.ac: (from bug pointed out by Hammad Majeed)
	Macro AC_DEFINE_UNQUOTED is now used to define GALIB_INCLUDEPATH,
	so that expansion of $GALIB is performed. Quotes were also added.

	* doc/README.texi: Uses @exdent to stop indentation of references
	in References section.

	* src/GE_ILLIGALSGA.cpp: Added Emacs reference flag at start, and
	defined _GE_ILLIGALSGA_CPP_.
	* src/GE_MITGALIB.cpp: Added Emacs reference flag at start, and
	defined _GE_MITGALIB_CPP_.

2004-10-12  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: Function
	print_individual() now uses a const attribute to its GAGenome
	argument.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: Function
	print_individual() now uses a const attribute to its GAGenome
	argument.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: Redefinition of
	signature of function print_individual.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: Call to
	print_individual() now uses proper best individual, gathered from
	statistics object.

2004-10-07  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.21.

	* configure.ac: (From bug pointed out by Michael O'Neill)
	Removed call to AC_CONFIG_SRCDIR for WRAPPERS source files.

2004-09-30  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.20.

	* doc/README.texi: Updated.
	* README: Now generated from doc/README.text, rather than
	doc/readme.texi, to include basic installation instructions.

	* src/CFGrammar.cpp: Removed include of <config.h>.
	* src/GEGrammarSI.cpp: Removed include of <config.h>.
	* src/GEMapper.cpp: Removed include of <config.h>.
	* src/Genotype.cpp: Removed include of <config.h>.
	* src/Grammar.cpp: Removed include of <config.h>.
	* src/Mapper.cpp: Removed include of <config.h>.
	* src/Production.cpp: Removed include of <config.h>.
	* src/Symbol.cpp: Removed include of <config.h>.
	* src/GEGrammar.cpp: Removed include of <config.h>.
	* src/GE_ILLIGALSGA.cpp: Removed include of <config.h>.
	* src/GE_MITGALIB.cpp: Removed include of <config.h>.
	* src/GESensibleInitialiser.cpp: Removed include of <config.h>.
	* src/Initialiser.cpp: Removed include of <config.h>.
	* src/Phenotype.cpp: Removed include of <config.h>.
	* src/Rule.cpp: Removed include of <config.h>.

2004-09-29  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.11beta4.

	* src/Tree,h: Implemented copy constructor.
	* src/Tree,h: Destructor no longer calls clear() for each subtree:
	delete operator does that already.

	* src/GEGrammar.cpp: All constructors now initialise maxWraps to 0;
	copy constructor copies the value of maxWraps from its argument.

	* src/Initialiser.h: Vector sizes is now seeds.
	* src/Initialiser.h: sizesPos is now seedsPos.
	* src/Initialiser.h: Method initSizes() is now initSeeds().
	* src/Initialiser.cpp: Corresponding changes.

	* src/Initialiser.h: New copy constructor.
	* src/Initialiser.cpp: Implementation of copy constructor.
	* src/GEGrammarSI.cpp: Copy constructor calls Initialiser's copy
	constructor.

	* doc/GE_MITGALIB.texi: Updated description of wrapper.

2004-09-28  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Genotype.h: Changed consistency of const application.
	* src/Genotype.cpp: Changed consistency of const application.
	* src/Phenotype.h: Changed consistency of const application.
	* src/Phenotype.cpp: Changed consistency of const application.
	* src/Mapper.h: Changed consistency of const application.
	* src/Mapper.cpp: Changed consistency of const application.
	* src/Symbol.h: Changed consistency of const application.
	* src/Symbol.cpp: Changed consistency of const application.
	* src/Production.h: Changed consistency of const application.
	* src/Production.cpp: Changed consistency of const application.
	* src/Rule.h: Changed consistency of const application.
	* src/Rule.cpp: Changed consistency of const application.
	* src/Grammar.h: Changed consistency of const application.
	* src/Grammar.cpp: Changed consistency of const application.
	* src/Tree.h: Changed consistency of const application.
	* src/CFGrammar.h: Changed consistency of const application.
	* src/CFGrammar.cpp: Changed consistency of const application.
	* src/GEGrammar.h: Changed consistency of const application.
	* src/GEGrammar.cpp: Changed consistency of const application.
	* src/Initialiser.h: Changed consistency of const application.
	* src/Initialiser.cpp: Changed consistency of const application.
	* src/GEGrammarSI.h: Changed consistency of const application.
	* src/GEGrammarSI.cpp: Changed consistency of const application.

	* doc/genotype.png: Newly introduced.
	* doc/genotype.txt: Newly introduced.
	* doc/phenotype.png: Newly introduced.
	* doc/phenotype.txt: Newly introduced.
	* doc/mapper.png: Newly introduced.
	* doc/mapper.txt: Newly introduced.
	* doc/rule.png: Newly introduced.
	* doc/rule.txt: Newly introduced.
	* doc/production.png: Newly introduced.
	* doc/production.txt: Newly introduced.
	* doc/symbol.png: Newly introduced.
	* doc/symbol.txt: Newly introduced.
	* doc/grammar.png: Newly introduced.
	* doc/grammar.txt: Newly introduced.
	* doc/tree.png: Newly introduced.
	* doc/tree.txt: Newly introduced.
	* doc/cfgrammar.png: Newly introduced.
	* doc/cfgrammar.txt: Newly introduced.
	* doc/gegrammar.png: Newly introduced.
	* doc/gegrammar.txt: Newly introduced.
	* doc/initialiser.png: Newly introduced.
	* doc/initialiser.txt: Newly introduced.
	* doc/gegrammarsi.png: Newly introduced.
	* doc/gegrammarsi.txt: Newly introduced.

2004-09-27  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: Updated description of methods.
	* doc/libGE.texi: New CFGrammar subsection.
	* doc/cfgrammar.fig: Newly introduced.
	* doc/cfgrammar.eps: Newly introduced.
	* src/GEGrammar.cpp: Updated description of methods.
	* doc/libGE.texi: New GEGrammar subsection.
	* doc/gegrammar.fig: Newly introduced.
	* doc/gegrammar.eps: Newly introduced.
	* src/Initialiser.cpp: Updated description of methods.
	* doc/libGE.texi: New Initialiser subsection.
	* doc/initialiser.fig: Newly introduced.
	* doc/initialiser.eps: Newly introduced.
	* src/GEGrammarSI.cpp: Updated description of methods.
	* doc/libGE.texi: New GEGrammarSI subsection.
	* doc/gegrammarsi.fig: Newly introduced.
	* doc/gegrammarsi.eps: Newly introduced.

	* doc/libGE.texi: Updated concept and function indexes.

	* src/Production.cpp: Updated description of methods.
	* doc/libGE.texi: New Production subsection.
	* doc/production.fig: Newly introduced.
	* doc/production.eps: Newly introduced.
	* src/Rule.cpp: Updated description of methods.
	* doc/libGE.texi: New Rule subsection.
	* doc/rule.fig: Newly introduced.
	* doc/rule.eps: Newly introduced.
	* src/Grammar.cpp: Updated description of methods.
	* doc/libGE.texi: New Grammar subsection.
	* doc/grammar.fig: Newly introduced.
	* doc/grammar.eps: Newly introduced.
	* src/Tree.h: Updated description of methods.
	* doc/libGE.texi: New Tree subsection.
	* doc/tree.fig: Newly introduced.
	* doc/tree.eps: Newly introduced.

2004-09-24  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Brief description of programming interface.
	* doc/libGE.texi: Removed old description of classes.

	* src/Genotype.cpp: Updated description of methods.
	* doc/libGE.texi: New Genotype subsection.
	* doc/genotype.fig: Newly introduced.
	* doc/genotype.eps: Newly introduced.
	* src/Phenotype.cpp: Updated description of methods.
	* doc/libGE.texi: New Phenotype subsection.
	* doc/phenotype.fig: Newly introduced.
	* doc/phenotype.eps: Newly introduced.
	* src/Mapper.cpp: Updated description of methods.
	* doc/libGE.texi: New Mapper subsection.
	* doc/mapper.fig: Newly introduced.
	* doc/mapper.eps: Newly introduced.
	* src/Symbol.cpp: Updated description of methods.
	* doc/libGE.texi: New Symbol subsection.
	* doc/symbol.fig: Newly introduced.
	* doc/symbol.eps: Newly introduced.

2004-09-23  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/allclasses.fig: Updated.
	* src/allclasses.eps: Updated.
	* src/allclasses.txt: Updated.
	* src/allclasses.pdf: Updated.
	* src/allclasses.png: Updated.

	* configure.ac: Call to AC_CONFIG_HEADERS with "src/libGEdefs.h"
	instead of "src/ge.h".
	* configure.ac: Removed appending of includes with AH_BOTTOM.
	* Makefile.am: libGEdefs.h added to GE_HEADERS.
	* src/ge.h: Newly introduced.
	* src/Genotype.h: #include"libGEdefs.h".
	* src/Phenotype.h: #include"libGEdefs.h".
	* src/Mapper.h: #include"libGEdefs.h".
	* src/Grammar.h: #include"libGEdefs.h".
	* src/Rule.h: #include"libGEdefs.h".
	* src/Production.h: #include"libGEdefs.h".
	* src/Symbol.h: #include"libGEdefs.h".
	* src/CFGrammar.h: #include"libGEdefs.h".
	* src/GEGrammar.h: #include"libGEdefs.h".
	* src/Initialiser.h: #include"libGEdefs.h".
	* src/GEGrammarSI.h: #include"libGEdefs.h".

2004-09-22  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Genotype.cpp: Replaced DEFAULT_BLOCK_SIZE with 100.
	* src/CFGrammar.cpp: Replaced DEFAULT_BLOCK_SIZE with 1024.
	* src/CFGrammar.cpp: Replaced INVALID_GRAMMAR_FILE with 1.
	* src/definitions.h: Removed.
	* Makefile.am: Removed definitions.h from GE_HEADERS.
	* src/Genotype.h: Removed include of "definitions.h".
	* src/Genotype.cpp: Removed include of "definitions.h".
	* src/Phenotype.h: Removed include of "definitions.h".
	* src/Phenotype.cpp: Removed include of "definitions.h".
	* src/Mapper.cpp: Removed include of "definitions.h".
	* src/Symbol.cpp: Removed include of "definitions.h".
	* src/Production.cpp: Removed include of "definitions.h".
	* src/Rule.cpp: Removed include of "definitions.h".
	* src/Grammar.cpp: Removed include of "definitions.h".
	* src/CFGrammar.cpp: Removed include of "definitions.h".
	* src/GEGrammar.cpp: Removed include of "definitions.h".
	* src/Initialiser.cpp: Removed include of "definitions.h".
	* src/GEGrammarSI.cpp: Removed include of "definitions.h".

	* src/GE_ILLIGALSGA.cpp: Replaced include of "../../src/GEGrammar.h"
	wih "GEGrammar.h".
	* src/GE_MITGALIB.cpp: Replaced include of "../../src/Mapper.h"
	wih "Mapper.h".

	* src/GE: Removed directory and all files within.

2004-09-21  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* configure.ac: Use of AC_DEFINE to define symbol GE_ILLIGALSGA.
	* configure.ac: config.h replaced with src/ge.h on call to
	AC_CONFIG_HEADERS.
	* configure.ac: Use of AH_BOTTOM to include all .h files from project
	onto template src/ge.h.in, which later becomes ge.h.
	* configure.ac: Use of AH_TOP and AH_BOTTOM to define _GE_H_.
	* Makefile.am: Removed conditional code at start to choose which
	include file to use. Added src/ge.h to GE_HEADERS definition.

	* src/GE_ILLIGALSGA.h: Moved from WRAPPERS/ILLIGAL.
	* src/GE_ILLIGALSGA.cpp: Moved from WRAPPERS/ILLIGAL.
	* src/GE_MITGALIB.h: Moved from WRAPPERS/MIT.
	* src/GE_MITGALIB.cpp: Moved from WRAPPERS/MIT.
	* Makefile.am: Changed location of above files.
	* src/GE_MITGALIB.h: Include of GALib headers through defined symbol
	GALIB_INCLUDEPATH; include of "ge.h".
	* src/GE_MITGALIB.cpp: Include of GALib headers through defined symbol
	GALIB_INCLUDEPATH.

2004-09-20  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* configure.ac: Use of AC_CHECK_FILE to check for the existence of
	the GAlib headers.
	* configure.ac: Use of AC_SUBST to define both CPPFLAGS and LIBS.
	* configure.ac: Use of AC_DEFINE to define symbol GALIB_INCLUDEPATH.

2004-09-17  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.11beta3.

	* src/Genotype.h: New method setMaxCodonValue().
	* src/Genotype.h: New private attribute maxCodonValue.
	* src/Genotype.cpp: Implementation of setMaxCodonValue().
	* src/Genotype.cpp: Changed implementation of setMaxCodonValue().
	* src/Genotype.cpp: Default constructor calls setMaxCodonValue().

	* src/definitions.h: Removed definition of STANDARD_MIN_FITNESS and
	MAX_GENOTYPE_LENGTH.
	* src/Genotype.h: Added definition of MIN_GENOTYPE_FITNESS,
	MAX_GENOTYPE_LENGTH and STANDARD_MAXCODONVALUE.
	* src/Phenotype.h: Added definition of MIN_PHENOTYPE_FITNESS.

	* src/Genotype.h: Order of parameters in default constructor changed.
	* src/Genotype.h: New constructor setting maxCodonValue added.
	* src/Genotype.cpp: Corresponding changes.

	* src/Mapper.h: Method setGenotypeMaxCodonValue() added.
	* src/Mapper.cpp: Corresponding changes.

	* src/GEGrammar.cpp: (from bug pointed out by Hammah Majeed)
	In method genotype2Phenotype(), if genotype is of length 0, exit
	the function (was causing memory leaks).

2004-09-15  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.11beta2.

2004-09-12  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Genotype.cpp: Call to vector<CodonType>(newLength) from default
	constructor, and vector bounds checking (as well as array bounds) when
	initialising Genotype from integer array.
	* src/Genotype.cpp: Call to vector<CodonType>(copy) from copy
	constructor, and removed call to operator=.
	* src/Phenotype.cpp: Call to vector<Symbol const*>() from default
	constructor.
	* src/Phenotype.cpp: Call to vector<Symbol const*>(copy) from copy
	constructor, and removed call to operator=.

	* src/Grammar.h: Destructor is now virtual.
	* src/CFGrammar.h: Destructor is now virtual.
	* src/GEGrammar.h: Destructor is now virtual.
	* src/GEGrammarSI.h: Destructor is now virtual.
	* src/Initialiser.h: Destructor is no longer pure.

2004-09-11  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Grammar.cpp: Call to vector<Rule>(copy) from copy constructor.
	* src/Rule.cpp: Call to vector<Production>(copy) from copy
	constructor.
	* src/Rule.cpp: In copy constructor, go through all members of lhs
	in copy individual and copy them.
	* src/CFGrammar.cpp: In copy constructor, go through all symbols in
	each production of each rule, and replace references to NTSymbols
	with references to the same symbols in the new grammar's lhs
	members of each rule.

	* src/GEGrammarSI.cpp: Copy constructor calls copy constructor for
	GEGrammar and default constructor for Initialiser.
	* src/Rule.cpp: Default constructor calls vector<Production*>
	default constructor.
	* src/Production.cpp: Default constructor calls vector<Symbol*>
	default constructor.

2004-09-10  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/allclasses.eps: Newly introduced.
	* doc/allclasses.pdf: Newly introduced.
	* doc/allclasses.png: Newly introduced.
	* doc/allclasses.txt: Newly introduced.

	* src/CFGrammar.cpp: (from bug pointed out by Michael O'Neill)
	Grammars created with editors such as emacs do not insert a newline
	character to separate lines, so if the last line does not have a
	newline specifically inserted, it crashes the BNF parser. Therefore
	the method readBNFFile() now inserts a "\n" as the last character of
	the program string to pass on to readBNFString().

	* src/Rule.h: Method clear() introduced.
	* src/Rule.cpp: Implementation of method clear().
	* src/Rule.cpp: Destructor calls method clear().
	* src/CFGrammar.cpp: Removed all calls to newRule.lhs.clear().

	* src/Tree.h: No longer includes "definitions.h".

2004-09-09  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.11beta1.

	* src/Rule.h: Methods push_back_lhs(), pop_back_lhs(), front_lhs(),
	back_lhs(), locate_lhs() and clear_lhs() removed; vector lhs is now
	public.
	* src/Rule.cpp: Corresponding changes.
	* src/Grammar.cpp: Corresponding changes.
	* src/CFGrammar.cpp: Corresponding changes.

	* src/Initialiser.h: Attribute sizesPos is now private.

	* src/GEGrammarSI.h: Methods initGenotype() and initPhenotype()
	replaced with method init().
	* src/GEGrammarSI.cpp: Corresponding changes.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/initfunc.cpp: Call to
	initGenotype() replaced with call to init().

2004-09-08  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Tree.h: Call to delete in method clear() to avoid memory leak.

	* src/GEGrammarSI.cpp: In method initGenotype(), if growTree() returns
	valid genotype, a call to genotype.setValid(true) is made.

	* src/Phenotype.cpp: Method c_str() now makes use of strdup() to avoid
	memory leak and returning invalid pointers.

	* src/Grammar.h: Newly introduced - replaces GEGrammar.h.
	* src/Grammar.cpp: Newly introduced - replaces GEGrammar.cpp.
	* src/CFGrammar.h: Derives from class Grammar; methods
	genotype2Phenotype() and phenotype2Genotype() are now pure virtual;
	method genotype2PhenotypeStep() removed.
	* src/CFGrammar.cpp: Respective changes.
	* src/GEGrammar.h: Newly introduced - derives from CFGrammar.
	* src/GEGrammar.cpp: Newly introduced - implements methods
	genotype2Phenotype(), phenotype2Genotype() and
	genotype2PhenotypeStep().
	* src/GEGrammarSI.h: Now derives from GEGrammar; include of
	"GEGrammar.h".
	* src/GEGrammarSI.cpp: Respective changes.
	* WRAPPERS/ILLIGAL/GE_ILLIGALSGA.cpp: Mapper used is now a GEGrammar.

	* src/Grammar.cpp: In default constructor, call to
	setValidGrammar(false).

	* src/Grammar.h: Private variable maxWraps and methods getMaxWraps()
	and setMaxWraps() removed. Arguments to constructors setting the
	number of maximum wrapping events removed.
	* src/Grammar.cpp: Corresponding changes.
	* src/CFGrammar.h: Arguments to constructors setting the number of
	maximum wrapping events removed.
	* src/CFGrammar.cpp: Corresponding changes.
	* src/GEGrammar.h: Private variable maxWraps and methods getMaxWraps()
	and setMaxWraps() added. Arguments to constructors setting the
	number of maximum wrapping events removed.
	* src/GEGrammar.cpp: Corresponding changes.

	* src/Grammar.h: Pure virtual methods readBNFFile(), readBNFString()
	and outputBNF() removed.

2004-09-07  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammarSI.h: Newly introduced - replaces GEMapper.h.
	* src/GEGrammarSI.cpp: Newly introduced - replaces GEMapper.cpp.
	* Makefile.am: References to GEMapper replaced with GEGrammarSI.

	* src/GE/STANDARD/ge.h: Include of <Tree.h>.
	* src/GE/STANDARD/ge.h: Include of <Initialiser.h>.
	* src/GE/STANDARD/ge.h: Include of <GEGrammarSI.h>.
	* src/GE/ILLIGALSGA/ge.h: Include of <Tree.h>.
	* src/GE/ILLIGALSGA/ge.h: Include of <Initialiser.h>.
	* src/GE/ILLIGALSGA/ge.h: Include of <GEGrammarSI.h>.
	* src/GE/MITGALIB/ge.h: Include of <Tree.h>.
	* src/GE/MITGALIB/ge.h: Include of <Initialiser.h>.
	* src/GE/MITGALIB/ge.h: Include of <GEGrammarSI.h>.
	* src/GE/ILLIGALSGA+MITGALIB/ge.h: Include of <Tree.h>.
	* src/GE/ILLIGALSGA+MITGALIB/ge.h: Include of <Initialiser.h>.
	* src/GE/ILLIGALSGA+MITGALIB/ge.h: Include of <GEGrammarSI.h>.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: Removed
	declaration of CFGrammar mapper.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: Removed
	declaration of CFGrammar mapper.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: Inserted declaration
	of global GEGrammarSI.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/initfunc.cpp: Using namespace
	std.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/initfunc.cpp: Function
	initfunc() renamed initfuncRandom().
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/initfunc.cpp: Function
	initfuncSI() introduced.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/initfunc.cpp: External
	declaration of GEGrammarSI mapper; include of <GE/ge.h>.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: External
	declaration of GEGrammarSI mapper.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: External
	declaration of GEGrammarSI mapper.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: Declaration of
	functions initFuncRandom() and initFuncSI(); the last one is now the
	assigned initialiser function.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/main.cpp: Initialisation of
	population size for sensible initialisation; include of <GE/ge.h>.

2004-09-06  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEMapper.cpp: In method initGenotype(), argument maximumDepth
	passed to growTree() is always getMaxDepth().
	* src/GEMapper.cpp: In method growTree(), "unmod" operator fixed.

2004-09-05  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEMapper.cpp: Code for method initGenotype().
	* src/GEMapper.cpp: Code for method growTree().

	* src/Mapper.h: genotype2Phenotype() and phenotype2Genotype() are now
	protected methods.
	* src/GEGrammar.h: genotype2Phenotype() and phenotype2Genotype() are now
	protected methods.
	* src/CFGrammar.h: genotype2Phenotype() and phenotype2Genotype() are now
	protected methods.

2004-09-04  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.h: Protected member derivationTree added.
	* src/CFGrammar.h: Method getDerivationTree() introduced.
	* src/CFGrammar.cpp: Implemented method getDerivationTree().

	* src/Production.h: Friend method operator<<() introduced.
	* src/Production.cpp: Friend method operator<<() implemented.
	* src/Genotype.cpp: Better implementation of  method
	operator<<().

2004-09-03  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEMapper.h: Methods initGenotype() and initPhenotype() no
	longer have argument Genotype or Phenotype structures.
	* src/GEMapper.cpp: initGenotype() and initPhenotype() use
	Mapper's genotype and phenotype structures directly; corresponding
	newGenotype and newPhenotype arguments removed.

	* src/Genotype.h: Typedef CodonType introduced.
	* src/Genotype.h: Genotype is now a vector of CodonType.
	* src/Genotype.h: Method getMaxCodonValue() introduced.
	* src/Genotype.cpp: Implemented method getMaxCodonValue().

	* src/Tree.h: Newly introduced.
	* Makefile.am: src/Tree.h added to GE_HEADERS.

2004-09-02  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.h: Method genotype2PhenotypeStep() introduced.
	* src/CFGrammar.cpp: Code for method genotype2PhenotypeStep().
	* src/CFGrammar.cpp: genotype2Phenotype() now calls
	genotype2PhenotypeStep().

	* src/GEMapper.h: Newly introduced to replace GESensibleInitialiser.
	* src/GEMapper.cpp: Newly introduced to replace GESensibleInitialiser.
	* Makefile.am: src/GEMapper.h added to GE_HEADERS.
	* Makefile.am: src/GEMapper.cpp added to libGE_a_SOURCES.
	* Makefile.am: Removed src/GEMapper.h from GE_HEADERS.
	* Makefile.am: Removed src/GEMapper.cpp from libGE_a_SOURCES.

2004-09-01  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GESensibleInitialiser.h: CFGrammar used instead of GEGrammar.
	* src/GESensibleInitialiser.cpp: CFGrammar used instead of GEGrammar.

	* src/CFGrammar.h: New method getStartRule().
	* src/CFGrammar.cpp: Implemented method getStartRule().

2004-08-31  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Rule.cpp: Default constructor uses setRecursive() and
	setMinimumDepth(), and minimumDepth is now set to INT_MAX>>1.
	* src/Rule.cpp: Copy constructor uses setRecursive() and
	setMinimumDepth().
	* src/Production.cpp: Default constructor uses setRecursive() and
	setMinimumDepth(), and minimumDepth is now set to INT_MAX>>1.
	* src/Production.cpp: Copy constructor uses setRecursive() and
	setMinimumDepth().

	* src/Rule.cpp: Useless "this" pointers removed.
	* src/Production.cpp: Useless "this" pointers removed.

	* src/Genotype.cpp: setValid() and setFitness() used instead of
	direct access to variables valid and fitness.

	* src/Genotype.h: Method operator=() removed.
	* src/Genotype.cpp: Method operator=() removed.

	* src/Rule.cpp: Direct iterators used; include of <vector>
	removed.
	* src/Production.cpp: Direct iterators used; include of <vector>
	removed.
	* src/Genotype.cpp: Direct iterators used; include of <vector>
	removed.
	* src/Phenotype.cpp: Direct iterators used; include of <vector>
	removed.

2004-08-30  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: In method genotype2Phenotype(), eternally
	recursive, non codon consumer productions are detected.

	* src/GEGrammar.h: GEGrammar now inherits vector<Rule> as well, and
	rules vector removed.
	* src/GEGrammar.cpp: References to rules vector replaced with direct
	calls to vector methods. Vector iterators replaced with direct
	iterators.
	* src/CFGrammar.cpp: References to rules vector replaced with direct
	calls to vector methods. Vector iterators replaced with direct
	iterators.
	* src/GESensibleInitialiser.cpp: References to mapper.rules.* replaced
	with mapper.*.

	* src/CFGrammar.cpp: Method outputGrammar() calls getValidGrammar()
	before outputing the grammar.

2004-08-27  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammar.h: Method getValidBNF() is now getValidGrammar().
	* src/GEGrammar.h: Method outputBNF() is now outputGrammar().
	* src/GEGrammar.h: Method setValidBNF() is now setValidGrammar().
	* src/GEGrammar.h: Attribute validBNF is now validGrammar.
	* src/GESensibleInitialiser.cpp: Now using method getValidGrammar().

	* src/GEGrammar.h: Method getStartSymbol() added.
	* src/GEGrammar.cpp: Code for method getStartSymbol().

	* src/CFGrammar.cpp: In method genotype2Phenotype(), iterators are
	used rather than indexes in vectors.

2004-08-26  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Initialiser.h: Method initSizes() is now protected.

	* src/GEGrammar.h: Variables maxWraps and validBNF are now private.
	* src/GEGrammar.h: Added protected method setValidBNF().
	* src/GEGrammar.cpp: Added implementation for method setValidBNF().
	* src/GEGrammar.cpp: All constructors make use of setValidBNF().
	* src/CFGrammar.cpp: In method ReadBNFString(), setValidBNF() is
	now used.

	* src/Initialiser.h: Removed variable mapper.
	* src/Initialiser.h: Removed all references to mapper.
	* src/Initialiser.cpp: Removed all references to mapper.
	* src/GESensibleInitialiser.h: Removed mapper from constructor.
	* src/GESensibleInitialiser.cpp: Removed mapper from constructor.

	* src/Initialiser.h: Protected variable sizesPos added.
	* src/Initialiser.cpp: In default constructor, sizesPos initialised.
	* src/Initialiser.h: Added method getIndex().
	* src/Initialiser.h: Added method setIndex().
	* src/Initialiser.h: Implemented method getIndex().
	* src/Initialiser.h: Implemented method setIndex().

	* src/Initialiser.h: Updated initGenotype() and initPhenotype()
	interface signatures.

2004-08-25  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Initialiser.h: Private variables randomSeed and sizes added.
	* src/Initialiser.h: Include of <vector> added.
	* src/Initialiser.h: Default constructor accepts second argument to
	set randomSeed.
	* src/Initialiser.cpp: Changed default constructor to accept second
	argument to set randomSeed, and to resize "sizes" vector.
	* src/Initialiser.h: Method getRandomSeed() added.
	* src/Initialiser.h: Method setRandomSeed() added.
	* src/Initialiser.cpp: Code for method getRandomSeed() added.
	* src/Initialiser.cpp: Code for method setRandomSeed() added.

	* src/Initialiser.h: Private virtual method initSizes() added.

	* src/GESensibleInitialiser.cpp: In method initGenotype(), added
	boundary and validity checks at start.

2004-08-20  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: In method updateRuleFields(), two iterations
	through all rules with calls to isRecursive() are made, to correctly
	update recursive rules and productions.

2004-08-19  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: In method isRecursive(), rule minimumDepth
	is updated per production.

	* src/CFGrammar.h: Added method clearRuleFields().
	* src/CFGrammar.cpp: Added code for method clearRuleFields().
	* src/CFGrammar.cpp: In method updateRuleFields(), clearRuleFields()
	is now used to reset minimumDepth and recursive attributes.

	* Makefile.am: Added Initialiser.h to GE_HEADERS.
	* Makefile.am: Added Initialiser.cpp to libGE_a_SOURCES.
	* Makefile.am: Added GESensibleInitialiser.h to GE_HEADERS.
	* Makefile.am: Added GESensibleInitialiser.cpp to libGE_a_SOURCES.
	* src/Initialiser.h: Newly introduced.
	* src/Initialiser.cpp: Newly introduced.
	* src/GESensibleInitialiser.h: Newly introduced.
	* src/GESensibleInitialiser.cpp: Newly introduced.

2004-08-18  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: Method isRecursive() goes through previously
	updated rules (to correctly set minimumDepth).
	* src/CFGrammar.cpp: In method updateRuleFields(), minimumDepth fields
	for each rule are reset prior to calling isRecursive().

2004-08-17  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.h: Added method isRecursive().
	* src/CFGrammar.cpp: Added method isRecursive().
	* src/CFGrammar.cpp: Wrote method updateRuleFields().
	* src/CFGrammar.cpp: In method readBNFString(): added call
	to updateRuleFields().

2004-08-12  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Production.h: Added enumeration type Recursive_type.
	* src/Production.h: Private variable recursive is now of type
	Recursive_type.
	* src/Production.h: Method getRecursive() now returns type
	Recursive_type.
	* src/Production.h: Method setRecursive() now takes argument of
	type Recursive_type.
	* src/Production.cpp: Method getRecursive() now returns type
	Recursive_type.
	* src/Production.cpp: Method setRecursive() now takes argument of
	type Recursive_type.
	* src/Rule.h: Private variable recursive is now of type
	Recursive_type.
	* src/Rule.h: Method getRecursive() now returns type
	Recursive_type.
	* src/Rule.h: Method setRecursive() now takes argument of
	type Recursive_type.
	* src/Rule.cpp: Method getRecursive() now returns type
	Recursive_type.
	* src/Rule.cpp: Method setRecursive() now takes argument of
	type Recursive_type.
	* src/Production.cpp: Default constructor initialises variables
	recursive and minimumDepth.
	* src/Production.cpp: Copy constructor initialises variables
	recursive and minimumDepth.
	* src/Rule.cpp: Default constructor initialises variable recursive.
	* src/Rule.cpp: Copy constructor initialises variable recursive.

2004-08-11  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Production.h: Private variable recursive added.
	* src/Production.h: Private variable minimumDepth added.
	* src/Production.h: Method getRecursive() added.
	* src/Production.h: Method getMinimumDepth() added.
	* src/Production.cpp: Code for method getRecursive() added.
	* src/Production.cpp: Code for method getMinimumDepth() added.
	* src/Rule.h: Method setRecursive() added.
	* src/Rule.h: Method setMinimumDepth() added.
	* src/Rule.cpp: Code for method setRecursive() added.
	* src/Rule.cpp: Code for method setMinimumDepth() added.
	* src/Production.h: Method setRecursive() added.
	* src/Production.h: Method setMinimumDepth() added.
	* src/Production.cpp: Code for method setRecursive() added.
	* src/Production.cpp: Code for method setMinimumDepth() added.

2004-08-10  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.10.

	* EXAMPLES/SantaFeAntTrail/GE_ILLIGALSGA/app.c:
	System call to gcc no longer uses the -Wno-deprecated flag.

	* doc/libGE.texi: Updated all interface descriptions to match changes
	made since version 0.10beta2, minus methods related to sensible
	initialisation.

	* README: Compatibility table has been updated.

2004-08-04  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.10beta3.

	* doc/libGE.texi: Miscellaneous corrections on "Writing Grammars"
	section.

2004-08-03  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Phenotype.cpp: Using Phenotype::iterator instead of explicit
	vector iterators.
	* src/Phenotype.cpp: Removed #include<vector>.

	* src/CFGrammar.cpp: In method genotype2Phenotype(), validity of
	phenotype and return value are the same.

	* src/CFGrammar.cpp: In method genotype2Phenotype(), non-terminals
	which are not defined still make it to invalid phenotypes, but
	phenotype is labeled invalid, and return value is false.

2004-08-02  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammar.h: Method validBNF() replaced with getValidBNF().
	* src/GEGrammar.h: Added variable validBNF.
	* src/GEGrammar.cpp: Method validBNF() replaced with getValidBNF().
	* src/GEGrammar.cpp: Constructors initialise validBNF.
	* src/CFGrammar.cpp: Replaced calls to validBNF() with getValidBNF().

	* src/GEGrammar.cpp: Cleared code from copy constructor.

2004-08-01  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Production.h: Method Production(unsigned int) added.
	* src/Production.cpp: Code for method Production(unsigned int) added.
	* src/Production.h: Copy constructor added.
	* src/Production.cpp: Code for copy constructor added.
	* src/Production.h: Method clear() added.
	* src/Production.cpp: Code for method clear() added.

	* src/GEGrammar.cpp: Destructor now deletes all Productions before
	deleting vector of Rules.

2004-07-31  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Rule.h: Added copy constructor.
	* src/Rule.cpp: Added code for copy constructor.
	* src/Rule.cpp: Added code for destructor.
	* src/Rule.cpp: Method push_back_lhs now receives argument as const.
	* src/Rule.cpp: Fixed code for clear_lhs().

	* src/Symbol.cpp: Added include file "definitions.h".
	* src/Rule.cpp: Added include file "definitions.h".

	* src/Rule.h: Added emacs-style definition header.
	* src/Rule.cpp: Added emacs-style definition header.

	* src/Production.h: Method ~Production() added.
	* src/Production.cpp: Firstly introduced.
	* Makefile.am:src/Production.cpp added to libGE_a_SOURCES.

2004-07-30  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: Added first pass to parser.
	* src/CFGrammar.cpp: Non-terminal symbols added to productions are now
	pointers to the corresponding non-terminal symbols, defined in Rule
	objects during the first pass.

2004-07-29  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Rule.h: Private variable recursive added.
	* src/Rule.h: Private variable minimumDepth added.
	* src/Rule.h: Method getRecursive() added.
	* src/Rule.h: Method getMinimumDepth() added.
	* src/Rule.cpp: Code for method getRecursive() added.
	* src/Rule.cpp: Code for method getMinimumDepth() added.

	* src/Rule.cpp: Placed closing #endif directive (from #ifndef at top)
	at the end of file.

	* src/CFGrammar.h: Method updateRuleFields() added.
	* src/CFGrammar.cpp: Definition of method updateRuleFields() added.

2004-05-26  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafestart.c: Include
	of <stdio.h>.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.c: (from bug
	pointed out by Dan Costelloe)
	System call to gcc no longer uses the -Wno-deprecated flag.

2004-05-25  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.10beta2.

	* WRAPPERS/ILLIGAL/GE_ILLIGALSGA.cpp: (from bug pointed out
	by Darwin Slattery)
	In method GE_ILLIGALSGA_getPhenotype(), Symbol* iterators are now
	properly incremented.

	* src/CFGrammar.cpp: (from bug pointed out by Darwin Slattery)
	Method outputBNF corrected, to use Symbol pointers.

	* src/GE/ILLIGALSGA+MITGALIB/ge.h: (from bug pointed out by David Power)
	Directive #ifdef __cplusplus used to avoid including c++ source code
	for ILLIGALSGA wrapper.
	* src/GE/ILLIGALSGA/ge.h: Directive #ifdef __cplusplus used to avoid
	including c++ source code for ILLIGALSGA wrapper.

2004-05-13  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.10beta1.

	* src/CFGrammar.cpp: In method readBNFString(), character '\' is now
	accepted to break lines in grammars.

	* src/CFGrammar.cpp: In method genotype2Phenotype(), no codon is read
	if there is only one production for the current rule.

2004-05-12  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.09.

	* doc/GE_ILLIGALSGA.texi: Updated description class
	diagram.

2004-05-11  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/SantaFeAntTrail/GE_ILLIGALSGA/app.cpp: Removed
	debugging instructions.

	* src/Phenotype.cpp: Updated method descriptions.

	* doc/GE_ILLIGALSGA.texi: Updated description of Phenotype
	interface.

2004-05-06  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.09beta3.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: Now using
	include file <GE/ge.h>.

	* Makefile.am: Use of conditionals to include the required GE/ge.h
	file in the package, which includes the include files from the
	installed wrappers.
	* src/GE/STANDARD/ge.h: Firstly introduced.
	* src/GE/ILLIGALSGA/ge.h: Firstly introduced.
	* src/GE/MITGALIB/ge.h: Firstly introduced.
	* src/GE/ILLIGALSGA+MITGALIB/ge.h: Firstly introduced.
	* EXAMPLES/SantaFeAntTrail/GE_ILLIGALSGA/app.cpp: Line including
	<GE/GE_ILLIGALSGA.h> removed.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: Line including
	<GE/GE_MITGALIB.h> removed.

2004-05-05  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.09beta2.

	* src/Phenotype.h: Method c_str() added.
	* src/Phenotype.cpp: Code for method c_str() added.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: In function
	objfunc(), c_str() is now used (in place of getString().c_str()).
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: In function
	objfunc(), c_str() is now used (in place of getString().c_str()).

2004-05-04  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Phenotype.h: Phenotype is now a vector of <Symbol const *>;
	include of "Symbol.h" added. Default constructor reduced to
	two arguments (validity and fitness). Member functions
	operator= removed.
	* src/Phenotype.cpp: Code of constructor changed to match
	changes above; documentation for constructor updated. Code for
	member functions operator= removed. Function operator<<
	updated, to iterate through Symbol* members; documentation
	for function updated.

	* src/Rule.h: lhs is now a vector of <Symbol*>.
	* src/Rule.h: Methods front_lhs(), back_lhs() and locate_lhs() all
	return Symbol*.
	* src/Rule.cpp: Methods front_lhs, back_lhs() and locate_lhs()
	updated to to match changes above.

	* src/CFGrammar.cpp: Method genotype2phenotype redesigned to use
	Symbol pointers, and to reflect changes to Phenotype class.

	* WRAPPERS/ILLIGAL/GE_ILLIGALSGA.cpp: Method
	GE_ILLIGALSGA_getPhenotype changed to reflect changes on Phenotype
	and Symbol classes.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: In function
	objfunc(), writing the phenotype code onto a file now makes use of
	the new method getString().
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: In function
	print_individual, phenotype is now printed using function operator<<.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: In function
	objfunc(), writing the phenotype code onto a file now makes use of
	the new method getString().
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: In function
	print_individual, phenotype is now printed using function operator<<.

2004-05-02  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.09beta1.

	* src/CFGrammar.cfg: Method genotype2phenotype corrected for use of
	new definitions of Rule, Production and Symbol.

	* WRAPPERS/ILLIGAL/GE_ILLIGALSGA.h: Method GE_ILLIGALSGA_readBNF
	replaced with methods GE_ILLIGALSGA_readBNFFile and
	GE_ILLIGALSGA_readBNFString.
	* WRAPPERS/ILLIGAL/GE_ILLIGALSGA.cpp: Method GE_ILLIGALSGA_readBNF
	replaced with methods GE_ILLIGALSGA_readBNFFile and
	GE_ILLIGALSGA_readBNFString.
	* doc/GE_ILLIGALSGA.texi: Updated to use method
	GE_ILLIGALSGA_readBNFFile.
	* doc/libGE.texi: ILLIGALSGA wrapper section updated to reflect
	replacement of method GE_ILLIGALSGA_readBNF with methods
	GE_ILLIGALSGA_readBNFFile and GE_ILLIGALSGA_readBNFString.
	* EXAMPLES/SantaFeAntTrail/GE_ILLIGALSGA/app.c: Replaced call to
	GE_ILLIGALSGA_readBNF with call to GE_ILLIGALSGA_readBNFFile.
	
2004-04-29  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/classes.fig: Updated class diagram.
	* doc/classes.eps: Updated class diagram.
	* doc/classes.pdf: Updated class diagram.
	* doc/classes.txt: Updated class diagram.

2004-04-28  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: Symbols are added to Rules, rather
	than pointers to created Symbols.
	* src/CFGrammar.cpp: Productions are added to Rules, rahter
	than pointers to created Productions.

2004-04-27  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Rule.h: Method clear_lhs introduced.
	* src/Rule.cpp: Method clear_lhs introduced.

	* src/Symbol.h: Copy constructor introduced.
	* src/Symbol.cpp: Copy constructor introduced.

2004-04-26  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Rule.h: lhs is now a vector of <Symbol>, not <Symbol*>.
	* src/Rule.h: Methods front_lhs, back_lhs and locate_lhs all
	return Symbol instead of Symbol*.
	* src/Rule.cpp: Methods front_lhs, back_lhs and locate_lhs all
	return Symbol instead of Symbol*.

2004-04-21  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/GEGrammar.h: Destructor introduced.
	* src/GEGrammar.cpp: Destructor introduced.
	* src/CFGrammar.h: Destructor introduced.
	* src/CFGrammar.cpp: Destructor introduced.
	* src/Symbol.h: Destructor introduced.
	* src/Symbol.cpp: Destructor introduced.
	* src/Rule.h: Destructor introduced.
	* src/Rule.cpp: Destructor introduced.

	* src/Rule.h: Method front_lhs() now returns Symbol* or
	Symbol const *.
	* src/Rule.cpp: Method front_lhs() now returns Symbol* or
	Symbol const *.
	* src/Rule.h: Method back_lhs() returns Symbol* or
	Symbol const *.
	* src/Rule.cpp: Method back_lhs() returns Symbol* or
	Symbol const *.
	* src/Rule.h: Method push_back_lhs(Symbol) is now
	push_back_lhs(Symbol*).
	* src/Rule.h: lhs is now a vector of <Symbol*>.
	* src/Rule.cpp: Method push_back_lhs got corrected call to
	lhs.push_back.


	* src/Rule.h: New method locate_lhs now introduced.
	* src/Rule.cpp: New method locate_lhs now introduced.

	* src/Production.h: Production is now a vector of <Symbol*>.

2004-04-18  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Symbol.h: Symbol class now inherits from class string, rather
	than vector<char>.
	* src/Symbol.h: Default constructor redesigned.
	* src/Symbol.h: Methods getString, setString and operator<< have been
	removed.
	* src/Symbol.cpp: Implementation of methods getString, setString and
	operator<< have been removed.

2004-04-17  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/ge.h: Firstly introduced; includes all include files.
	* Makefile.am: src/ge.h added to GE_HEADERS.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp:
	<GE/ge.h> included in place of <GE/CFGrammar.h>.

2004-04-17  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.08.

	* doc/libGE.texi: Copyright note now uses years 2003-2004.

	* doc/libGE.texi: Symbol class updated with latest changes.

2004-03-12  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.08beta5.

	* src/CFGrammar.cpp: (from bug pointed out by Michael O'Neill)
	Insertion of " " character is now done through the creation of a
	new Symbol object, in method ReadBNFString.
	* src/CFGrammar.cpp: Increase in size of Phenotype object is now
	correct (an extra unit of size was being added due to the insertion
	of ' ' characters, which is no longer happening).

2004-03-09  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.08beta4.

	* src/CFGrammar.cpp: Tokens separated by one or more spaces or tabs
	provoke the insertion of a " " terminal symbol in grammar, when read
	by the parser; tokens not separated by spaces or tabs generate
	symbols not separated, when creating a phenotype string.

2004-03-07  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Symbol.h: Constructor now accepts a third argument: the
	type of the symbol.
	* src/Symbol.cpp: Constructor now accepts a third argument: the
	type of the symbol.

2004-03-04  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: Escaped < and > symbols are now
	accepted within terminal and non-terminal symbols.

2004-03-03  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.08beta3.

	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/*: Removed all unnecessary
	files, as these were being included in the package.

2004-03-02  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.08beta2.

	* src/CFGrammar.cpp: (from bug pointed out by Michael O'Neill)
	Method readBNFString now sets TSymbol type properly to all
	terminal symbols.

2004-03-01  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.08beta1.

	* config/mdate-sh: Installed (required by automake-1.8).
	* config/texinfo.tex: Installed (required by automake-1.8).

2004-02-28  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: Method readBNFString completely rewritten, now
	using a character by character read and state parser.

2004-02-26  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.07.

	* src/CFGrammar.cpp: (from bug pointed out by Eoin Ryan)
	Include <cstdio> for fseek and other functions (ReadHat conplains
	about these not being present).

	* doc/libGE.texi: FAQ section updated.

2004-02-25  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.06.

2004-02-03  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/readme.texi: Section about compatibility with other systems.
	* doc/readme.texi: Variables MANDRAKEVERSION and GCCVERSION defined.

2004-01-30  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/Genotype.h: Documented class.
	* src/Phenotype.h: Documented class.
	* src/Mapper.h: Documented class.
	* src/GEGrammar.h: Documented class.
	* src/CFGrammar.h: Documented class.
	* src/Rule.h: Documented class.
	* src/Production.h: Documented class.
	* src/Symbol.h: Documented class.
	* src/Genotype.cpp: Documented methods.
	* src/Phenotype.cpp: Documented methods.
	* src/Mapper.cpp: Documented methods.
	* src/GEGrammar.cpp: Documented methods.
	* src/CFGrammar.cpp: Documented methods.
	* src/Rule.cpp: Documented methods.
	* src/Symbol.cpp: Documented methods.
	* doc/libGE.texi: Added more method documentations, and minor
	formatting modifications.

2004-01-12  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/CFGrammar.cpp: (from bug pointed out by Michael O'Neill)
	Method readBNF(fstream*) now uses C I/O functions rather
	than C++.
	* src/GEGrammar.h: Method readBNF(fstream*) renamed to
	readBNFFile(char const *).
	* src/GEGrammar.h: Method readBNF(char const *) renamed to
	readBNFString(char const *).
	* src/CFGrammar.h: Method readBNF(fstream*) renamed to
	readBNFFile(char const *).
	* src/CFGrammar.h: Method readBNF(char const *) renamed to
	readBNFString(char const *).
	* src/CFGrammar.cpp: Removed <string> and <fstream> includes.
	* src/CFGrammar.cpp: Method readBNF(fstream*) renamed to
	readBNFFile(char const *).
	* src/CFGrammar.cpp: Method readBNF(char const *) renamed to
	readBNFString(char const *).
	* src/definitions.h: Symbol INVALID_GRAMMAR_FILE defined.
	* WRAPPERS/ILLIGAL/GE_ILLIGALSGA.cpp: Method GE_ILLIGALSGA_readBNF
	now calls readBNFFile() method.
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-gcc.cpp: Method
	app_init() now calls method readBNFFile().
	* EXAMPLES/SantaFeAntTrail/GE_MITGALIB/santafe-slang.cpp: Method
	app_init() now calls method readBNFFile().
	* doc/libGE.texi: Interface to classes GEGrammar and CFGrammar updated
	according to the above changes.
	* doc/libGE.texi: Section "Error Codes" and section "Size Limits"
	updated with symbols INVALID_GRAMMAR_FILE and DEFAULT_BLOCK_SIZE.

	* doc/libGE.texi: Minor formatting and grammatical corrections.

	* doc/readme.texi: Paragraph about documentation format.

	* doc/classes.png: Firstly introduced.
	* doc/ge-ex.png: Firstly introduced.
	* doc/ge.png: Firstly introduced.
	* doc/packages.png: Firstly introduced.

	* doc/libGE.texi: Major restructuring of nodes in Chapter "Examples".

2004-01-08  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.06beta2.

	* src/CFGrammar.cpp: (from bug pointed out by Michael O'Neill)
	Type (char*) used for line instead of string.
	* src/CFGrammar.cpp: (from bug pointed out by Michael O'Neill)
	Method read() used instead of getline().
	* src/definitions.h: Symbol DEFAULT_BLOCK_SIZE defined.

2003-12-11  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* Makefile.am: EXTRA_DIST symbol for doc/*.texi, doc/*.pdf,
	doc/*.eps and doc/*.txt declared.

2003-12-10  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* GRAMMARS: Deleted directory.
	* Makefile.am: libGE_a_SOURCES no longer includes
	GRAMMARS/SantaFe.bnf and GRAMMARS/SymbolicRegression.bnf.
	* Makefile.an: rules for building SantaFe.bnf and
	SymbolicRegression.bnf removed.

	* Makefile.am: EXTRA_DIST symbol for README files in WRAPPERS
	directories declared.
	* Makefile.am: EXTRA_DIST symbol for EXAMPLES/SantaFeAntTrail/*
	declared.
	* Makefile.am: libGE_a_SOURCES no longer includes README files.
	* Makefile.am: rule for building README files removed.

2003-12-02  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Section 5 finished.

2003-12-01  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Section 5.1.3 finished.

2003-11-27  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* EXAMPLES/GE_MITGALIB/*: Firstly introduced.

	* doc/libGE.texi: Sections 5.1 and 5.1.1.

2003-11-25  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* WRAPPERS/ILLIGAL/GE_ILLIGALSGA.cpp: struct individual is now
	equal to the original sga-c code structure.

	* doc/libGE.texi: Section 4.1 finished.

2003-11-25  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Chapter 3 finished.

2003-11-24  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Chapter 2 finished.
	* doc/install.texi: Unpackaging instructions added.

2003-11-14  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.06beta1.

2003-10-17  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* src/*: (from bug pointed out by Simon Mackey)
	All files using unsigned type now use unsigned int.
	* src/*: (from bug pointed out by Simon Mackey)
	All files using string type now #include<string>.

2003-10-15  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* configure.ac: Changes relative to the introduction of the
	WRAPPERS directory.
	* Makefile.am: Changes relative to the introduction of the
	WRAPPERS directory.
	* WRAPPERS/ILLIGAL/GE_IILIGALSGA.h: This is this file's
	new location.
	* WRAPPERS/ILLIGAL/GE_IILIGALSGA.cpp: This is this file's
	new location.
	* WRAPPERS/ILLIGAL/GE_IILIGALSGA.cpp: Include path for CFGrammar.h has
	been changed because of the new directory structure.

2003-09-26  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* doc/libGE.texi: Firstly introduced.
	* doc/fdl.texi: Firstly introduced.
	* Makefile.am: info_TEXINFOS macro is used for texinfo documentation
	file.

2003-09-25  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* ChangeLog: Stricter implementation of GNU format
	standard.

2003-09-05  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.05.

	* MIT/README: Corrected the usage of --with-MITGALIBINCLUDES.

	* configure.ac: Configure script now terminates if GAlib headers are
	not found.

	* MIT/GE_MITGALIB.cpp: Conditional include of GALIB_INCLUDEPATH
	removed; <ga/ga.h> always used now.
	* configure.ac: GALIB_INCLUDEPATH symbol no longer defined.
	* configure.ac: All test programs now include <ga/ga.h>.

	* configure.ac: (from bug pointed out by Michael O'Neill)
	CPPFLAGS is used to point to base directory of GALIB, when using
	--with-MITGALIBINCLUDES flag.

	* configure.ac: GElib specific command-line options receive options
	yes/no, rather than YES/NO.

2003-09-04  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* Makefile.am: (from bug pointed out by Michael O'Neill)
	libGE_a_SOURCES includes ILLIGAL/README and MIT/README, so that
	make dist will include these on the resulting package (they were
	not shipped with version 0.04).

2003-09-03  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.04.

	* Makefile.am: GEdir and GE_HEADERS are defined rather than using
	pkginclude_HEADERS, which defaults to $(includedir)/tarname.

	* Makefile.am: AM_CFLAGS defines the flag -g rather than -pg.

	* Makefile.am: libGE_a_SOURCES includes contents of GRAMMARS
	directory, so that make dist will include these on the resulting
	package.
	* GRAMMARS/SantaFe.bnf: Firstly introduced.

	* configure.ac: Obsolete macro call AM_INIT_AUTOMAKE(GE,0.04) has been
	replaced by AM_INIT_AUTOMAKE (AC_INIT already specifies package name
	and version).
	* configure.ac: AC_INIT now specifies package as libGE.

	* configure.ac: AC_COMPILE_IFELSE is used in place of AC_LINK_IFELSE
	to check for GAlib headers.

2003-09-02  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* MIT/GE_MITGALIB.h: Does not include <ga/ga.h>, to allow the user to
	change the location of GAlib without reinstalling libGE.
	* MIT/GE_MITGALIB.cpp: Conditional include of GALIB_INCLUDEPATH.
	* src/*.h: All header files define the use of namespace std.
	* src/*.cpp: All cpp files include <config.h> at the start, and
	"definitions.h" at the end.
	* src/*: All source files now include the headers they require, even
	if these might have already been included by previous files.

	* configure.ac: LIBS variable is only set after AC_LANG(C++) and all
	$GA_INCLUDE tests, so that test programs will not try to link to
	GALib.

2003-09-01  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* configure.ac: AC_DEFINE(GALIB_INCLUDEPATH,...) and
	AC_DEFINE_UNQUOTED(GALIB_INCLUDEPATH,...) are used to define symbol
	GALIB_INCLUDEPATH in config.h.

	* configure.ac: Options --with-MITGALIBINCLUDES=path and
	--with-MITGALIBLIBS=path.

	* ILLIGAL/README: New version.

2003-08-28  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* AUTHORS: New version.
	* README: New version.
	* NEWS: New version.
	* TODO: New version.
	* BUGS: New version.

	* configure.ac: MITGALIB checks (headers and lib).

2003-08-17  Miguel Nicolau  <miguel.nicolau@ul.ie>

	* MIT/GE_MITGALIB.h: Firstly introduced.
	* MIT/GE_MITGALIB.cpp: Firstly introduced.

2003-08-13  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.03.

	* src/CFGrammar.cpp: Method genotype2phenotype() improved.

2003-06-23  Miguel Nicolau  <miguel.nicolau@ul.ie>

	Version 0.02.

	* Makefile.am: Firstly introduced.
	* configure.ac: Firstly introduced.
	* AUTHORS: Firstly introduced.
	* ChangeLog: Firstly introduced.
	* COPYING: Firstly introduced.
	* INSTALL: Firstly introduced.
	* NEWS: Firstly introduced.
	* README: Firstly introduced.

 
